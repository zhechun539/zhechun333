# 折椿个人作品集项目概述

当前版本：`v01.40`

本文档由原《个人作品集网站制作进度与交接说明》和《网站制作进度与功能说明》整理迁移而来，用于帮助新协作者快速理解项目。内容以当前代码为准，已剔除过期实现、旧仓库信息、本机路径和私人联系方式。

## 1. 项目定位

这是一个面向求职展示的单页新媒体运营作品集。公开名称为“折椿”，目标方向包括内容策划、账号运营、品牌内容编辑和产品运营。

网站重点传达：

- 内容策划与选题判断
- 小红书、公众号、短视频等平台运营经验
- 项目协同、互动承接、数据复盘和转化意识
- 审美判断、内容组织与品牌表达能力
- 将 AI 用于内容、视觉和产品实践的能力

文案不主动强调毕业年份或经验年限，优先展示可验证的项目过程和成果。

## 2. 当前状态

- 单页作品集主体已经完成。
- 页面包含首屏、经历、精选项目、优势和联系模块。
- 当前公开名称为“折椿”。
- 页面保留求职邮件入口，不展示手机号和微信号。
- 项目可以执行生产构建，并通过 GitHub Actions 发布到 GitHub Pages。
- 线上地址为 `https://zhechun539.github.io/zhechun333/`。
- 首次正式发布前仍需人工复核作品截图内的账号、二维码和业务数据。

## 3. 技术栈

- React 19
- Vite 8
- Lucide React
- 原生 CSS
- Web Audio API
- GitHub Actions
- GitHub Pages

全站使用系统宋体栈，不依赖外部字体服务。项目是纯前端静态网站，不包含后端、数据库、登录、支付或用户数据收集功能。

## 4. 运行、构建与发布

安装依赖：

```powershell
npm.cmd install
```

本地开发：

```powershell
npm.cmd run dev -- --port 5174 --strictPort
```

也可直接双击根目录的 `启动本地预览.bat`：首次运行会安装依赖，随后启动本地开发服务器并自动打开预览页；保持命令窗口打开，按 `Ctrl+C` 停止服务。

本地预览路径：

```text
http://127.0.0.1:5174/zhechun333/#home
```

生产构建：

```powershell
npm.cmd run build
```

构建产物位于 `dist/`，不手动维护、不提交 Git。`vite.config.js` 当前使用 `base: '/zhechun333/'`，与 GitHub 仓库名一致；修改仓库名、自定义域名或部署路径时，必须同步调整并检查所有资源地址。React 中的公开素材通过 `import.meta.env.BASE_URL` 生成地址，CSS 中的素材使用可由 Vite 构建转换的相对路径，避免 GitHub Pages 子目录部署时请求站点根路径。

`.github/workflows/deploy-pages.yml` 在 `main` 分支更新后执行：

1. 检出源码。
2. 安装 Node.js 和 npm 依赖。
3. 执行生产构建。
4. 上传 `dist/`。
5. 发布 GitHub Pages。

## 5. 页面结构

### 5.1 首屏 `#home`

- 使用低饱和自然系杂志封面构图。
- 主视觉为森林与白鸟，不使用人物照片。
- 主图：`public/assets/hero-forest-photo.png`。
- 辅助视觉：`hero-stream-flowers.png`、`hero-lily-water.png`、`hero-wisteria-canopy.png`。
- 标题使用公开名称“折椿”，副标题为 `Editorial Content Portfolio`。
- 主要入口为“精选项目”和“联系我”。
- 包含颗粒、自然光影、稀疏雨滴、柔和显现和克制的图像运动。
- `RainMusicButton` 使用 Web Audio API 实时合成雨幕、环境和弦与稀疏旋律，不加载第三方音乐文件；受浏览器自动播放限制，必须由用户点击开启。

### 5.2 关于与履历 `#about`

- 个人照片：`public/media/pdf-images/full_p02_i01_Im73.jpg`。
- 照片与求职方向组成互动信息面板。
- “运营 / 策划 / 产品”标签可切换能力说明。
- 邮箱入口整合进求职方向信息面板的标题行，与“求职方向”标签并列；不再在履历上方单独占用一行。
- 桌面端有轻微指针景深跟随和柔光反馈。
- 平板端采用左图右文，窄屏改为上下排列。
- 履历采用无卡片的职业索引表，区分正式工作、实习、线上兼职和阶段性项目；每段保留时间、岗位、类型、公司，并以独立的“职责 / 成果”信息行展示一条最能吸引招聘者的工作动作或可验证结果。摘要由现有案例事实压缩而来，不受旧段落隐藏规则影响；常见笔记本端由左侧人物面板决定双栏整体高度，右侧七条履历在同一高度内等分并与人物面板精确齐底，摘要保持单行；较窄视口恢复自然高度和最多两行摘要，避免压缩可读性。详细职责仍在下方项目案例中展开，各行按顺序轻量入场，并在低动态设置下停用动画。

### 5.3 经历总览与案例窗口 `#projects`

当前以内容运营岗位相关性排序，包含：

1. 品牌自媒体运营
2. 小红书种草协同
3. 教育类小红书获客
4. B 端软件内容运营
5. 旅游攻略内容引流
6. 热点传播与官方账号
7. 个人账号运营
8. 内容写作与补充作品
9. 不牛马厨房 AI 产品实践（补充能力）

章节只保留“项目经历”一个中文标题，以单行编辑式标题和细分隔线直接进入案例索引，不再重复显示眉题、“代表项目”或说明段落。项目标题与案例索引共用和上方照片、履历一致的居中内容母版：桌面最大内容宽度为 1216px，横向边距跟随全站 `20px—72px` 自适应间距，手机保持约 20px 安全边距，不再使用贴边满宽布局。默认页按内容属性分为两层：常见笔记本端将前八项岗位直接相关案例排成完整的 4×2 等高矩阵，第 09 项“不牛马厨房”作为独立 AI 产品实践在末尾独占一整行，并使用图片区与说明区并排的横向附录样式；平板与手机端前八项使用 2×4 双列矩阵，第 09 项同样始终横跨全部列，当前展示 800 道家常菜。第 09 项在窄屏使用独立的编辑式文字区：图片区与说明区由细竖线分隔，类型、项目名和成果脚注形成三层基线，避免沿用普通卡片的松散文字堆叠。项目区上下留白与标题区间距已压缩，使相邻板块衔接更自然。点击卡片后，在总览上方打开独立的案例阅读窗口；桌面端居中显示，手机端接近全屏并保留窗口边界。窗口支持右上角紧凑圆角方形关闭按钮、点击窗口外遮罩关闭、`Escape` 和浏览器返回键；关闭按钮完整收在标题栏安全边距内，键盘聚焦时显示单层方形焦点环；打开时锁定背景滚动并将焦点移到关闭按钮，关闭后恢复原卡片焦点。窗口顶部使用“项目 06 / 09”式中文位置标识，不再使用 `CASE STUDY`。前八项运营项目继续使用案例扉页、主图、项目概述、成果数据和“完整材料”折叠层；第 09 项 AI 产品使用专属产品案例窗口，不再显示折叠条，原扉页标题、完整海报、项目概述、结果数据与原折叠层中的产品命题、实现说明、在线入口和原创 Skill 被整合为连续主内容，并默认启用窗口内纵向阅读。AI 产品海报不使用白色展示框，按原始比例直接显示；桌面端为海报建立独立的居中视觉栏，海报与右侧叙事共享视觉中线并按视口适度缩放，窄屏则回到自然单栏；产品价值区不再展示疾病与口味标签，而是以“核心痛点 / 核心功能 / 产品特色”说明选择困难、800 道家常菜、随机转盘、偏好筛选和转盘宠物反馈。弹窗动态创建的 AI 视觉补充模块直接显示，不复用主页面的滚动入场状态；没有外链的普通案例只显示“完整介绍”，不做错误提示。项目图片在 `src/App.jsx` 的 `projectImages` 中配置，文字与链接在项目数据中维护。

### 5.4 优势 `#strengths`

- 六步流程：内容判断、选题策划、内容制作、发布运营、互动承接、数据复盘。
- 能力方向：内容策略、互动转化、多平台节奏、项目协同、基础视觉与剪辑。
- 桌面端为紧凑横向流程与三列能力矩阵，手机端采用两列流程和一至两列能力矩阵；去除大面积玻璃卡片，用细分隔线与小尺寸图标组织层级。
- 模块保留 AI 使用相关表达，可继续补充具体工具、工作流或案例。

### 5.5 联系 `#contact`

- 作为整页收尾，保留求职表达和邮件联系入口。
- 不展示手机号和微信号。

补充：第 09 项的 AI 表述仅用于说明辅助制作流程，用户侧功能为餐食决策、随机转盘和偏好筛选；桌面端海报与叙事采用均衡双栏。普通案例的原始作品链接直接置于主图右侧项目摘要下方，不再占用底部折叠区域。

## 6. 已实现功能

### 导航与页面反馈

- 桌面端固定完整导航，移动端使用菜单按钮。
- 滚动时自动高亮当前板块，并使用 `aria-current="page"` 标识。
- 导航底部显示页内阅读进度。
- 离开首屏后显示返回顶部按钮。
- 锚点使用平滑滚动。
- 经历、项目、优势和联系内容首次进入视口时自然显现。

### 项目轮播与灯箱

- 当前选中的项目包含当前主图、前后预览、左右切换和缩略图条；未选项目只加载紧凑索引。
- 只有一张纵向主图的项目使用明确的响应式展示高度，并同时限制图片宽高、保持原比例完整居中，不用容器裁切换取铺满效果。
- 当前缩略图切换后自动滚动到视觉中心。
- 项目截图使用懒加载。
- 点击当前主图可打开整图灯箱。
- 灯箱支持关闭按钮、点击图片外的深色留白或说明区域、`Escape`、左右按钮和键盘方向键；点击图片本身不会误关。
- 手机端按图片比例自适应，左右控制区保持可操作。

### 原始作品链接

- 原始作品链接使用横向杂志索引结构，依次展示作品索引抬头、作品/平台数量，以及由左侧平台信息栏和右侧流动作品区组成的平台章节；521px 以上每个平台横向展开，作品按三列流动，平台内容量决定章节自然高度；520px 以下平台标题回到上方、作品使用两列，减少纵向篇幅并充分利用弹窗宽度。
- 只有一个外部地址的项目不显示作品索引和平台计数，改用紧凑的在线产品入口：直接呈现产品名、域名与“打开在线体验”主按钮；桌面端横向排列，手机端上下排列。
- 展示作品名称，不直接堆叠长网址；每条作品行保留独立外链箭头、至少 44px 操作高度，以及悬停、按压和键盘焦点反馈。
- 新标签页链接使用 `noopener` 和 `noreferrer`；部分作品短链接按原始资料保留 HTTP。
- 桌面及中等窄屏使用统一的平台标签轨道与三列作品内容场，形成有基准线的非对称编辑节奏；小手机改为平台标题在上、作品两列，不采用等大卡片、瀑布流或跨平台混排。
- 链接区延续深绿文字、浅绿纸面反馈和细金色操作提示，不再使用大圆角玻璃容器。

### AI 辅助视觉展示

- 品牌自媒体项目包含独立的 AI 辅助视觉区。
- 说明 AI 在视觉构思、场景生成和素材延展中的用途。
- 示例图可以进入同一灯箱查看。

### 响应式与可访问性

- 核心响应式区间覆盖桌面、平板和手机，重点关注 `960px`、`760px`、`680px` 和更窄屏幕。
- 交互元素具备键盘焦点和语义化辅助说明。
- `prefers-reduced-motion: reduce` 下关闭或压缩非必要动画，内容仍完整可用。
- `index.html` 包含中文语言、viewport、description 和页面标题。

## 7. 视觉系统与用户偏好

整体方向：低饱和自然系、生活方式杂志感、纸质颗粒、柔和毛玻璃和自然光影。

推荐关键词：

- 鼠尾草绿、雾蓝、米白、浅灰绿
- 柔焦、胶片颗粒、轻量拼贴
- 真实摄影感、克制动效、自然节奏

避免：

- 科技风、赛博风、强渐变和复杂 3D
- 卡通感、模板化卡片和纯视觉炫技
- 首页人物主视觉
- 文字重叠、英文堆叠和图片堆砌
- 明显双层框、硬边框和无意义循环动画

按钮和关键操作优先使用中文。动画要表达状态、空间关系或操作反馈，不能只为“看起来会动”。

## 8. 素材与代码位置

- `index.html`：网页标题、描述和入口节点。
- `src/main.jsx`：React 入口。
- `src/App.jsx`：导航、经历、项目、链接、轮播、灯箱、音频和主要组件逻辑。
- `src/index.css`：视觉系统、布局、断点、动效和低动态模式。
- `public/assets/`：当前首屏使用的自然视觉素材。
- `public/media/ai-design/`：AI 辅助视觉示例。
- `public/media/pdf-images/`：当前项目实际引用的作品截图。
- `.github/workflows/deploy-pages.yml`：Pages 自动部署。
- `AGENTS.md`：AI 协作、视觉和验收规范。
- `项目概述.md`：当前产品与技术事实。
- `项目进度表.md`：版本化修改记录。

未使用素材保存在本地 `.local-unused-assets/`，旧文档原稿保存在 `.local-docs/`；两者均被 Git 忽略。

## 9. 维护入口

- 修改经历、项目、指标、能力和链接：编辑 `src/App.jsx` 对应数据与组件。
- 修改视觉、布局、断点和动效：编辑 `src/index.css`。
- 修改标题和 SEO 描述：编辑 `index.html`。
- 新增项目截图：放入 `public/media/pdf-images/`，并在 `projectImages` 中显式绑定。
- 新增普通视觉素材：放入 `public/assets/`，并通过 `assetUrl`、模块导入或 Vite 可处理的 CSS 路径引用。
- 不要手动修改 `dist/`；每次交付前重新执行构建。
- 修改功能或结构后，同步更新本文档；任何修改都要同步 `项目进度表.md`。

## 10. 验收要求

每次页面改动至少检查：

- 生产构建是否成功。
- 桌面端和移动端是否横向溢出。
- 首次加载、滚动、菜单、轮播、灯箱、外链和邮件入口。
- 控制台是否出现错误或资源 404。
- 动效是否自然，并兼容 `prefers-reduced-motion`。
- 新增素材是否真正被引用，是否包含不应公开的信息。

## 11. 后续事项

- 首次发布前人工检查作品截图中的账号、二维码、用户信息和业务数据。
- 定期复核外部短链接是否仍有效，避免失效跳转或目标变化。
- 可补充 favicon、社交分享图和更完整的分享元信息。
- 可继续细化项目目标、成果标签和 AI 使用案例。
- 可进一步强化联系页的整屏收尾效果，但应保持当前视觉体系。
