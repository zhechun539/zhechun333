# 折椿个人作品集项目概述

当前版本：`v04.78`

本文档由原《个人作品集网站制作进度与交接说明》和《网站制作进度与功能说明》整理迁移而来，用于帮助新协作者快速理解项目。内容以当前代码为准，已剔除过期实现、旧仓库信息、本机路径和私人联系方式。

## 1. 项目定位

这是一个面向求职展示的菜单式新媒体运营作品集。公开名称为“折椿”，目标方向包括内容策划、账号运营、品牌内容编辑和产品运营。

网站重点传达：

- 内容策划与选题判断
- 小红书、公众号、短视频等平台运营经验
- 项目协同、互动承接、数据复盘和转化意识
- 审美判断、内容组织与品牌表达能力
- 将 AI 用于内容、视觉和产品实践的能力

文案不主动强调毕业年份或经验年限，优先展示可验证的项目过程和成果。

## 2. 当前状态

- 菜单式作品集主体已经完成，五个一级模块按独立视图切换展示。
- 页面包含首屏、经历、精选项目、优势和联系模块。
- 每次重新打开或刷新站点时先展示轻量加载层：背景复用经历页的浅绿纸面底纹与柔和渐变，三只半透明珠光白蝴蝶与小猫同时启程，统一朝右上方沿页面上方起伏轨迹飞行并自然拍翼；“请稍等，作品集正在加载”引导语位于磨砂大框上方，框内小猫沿细横线向前行走，实时百分比固定在线下方；首页会先在加载层后方完成真实挂载、关键图片解码、字体就绪与首帧绘制，之后加载层才同步淡出并进入 `#home`。
- 当前公开名称为“折椿”。
- 页面保留求职邮件入口，并提供面向作品集建议、能力建议、合作邀约与面试邀约的联系表单；不展示手机号和微信号。
- 项目可以执行生产构建，并通过 GitHub Actions 发布到 GitHub Pages。
- 发布图片已统一优先使用高质量 WebP：透明 PNG 素材保留 Alpha 通道无损转换，实际显示尺寸较小的透明物件按约 2 倍显示尺寸下采样，项目截图与 AI 示例按高质量 WebP 发布；项目胶片只在进入经历页后预加载首批缩略图，其余截图继续按需加载。唱片机音乐使用 128 kbps MP3 派生文件，原始资源留在本地忽略目录，不进入发布产物。
- 线上地址为 `https://resume.zhechun.space/`；GitHub 项目地址 `https://zhechun539.github.io/zhechun333/` 同样可访问。
- 首次正式发布前仍需人工复核作品截图内的账号、二维码和业务数据。

## 3. 技术栈

- React 19
- Vite 8
- Lucide React
- Three.js
- Framer Motion
- 原生 CSS
- Web Audio API + HTMLAudioElement
- GitHub Actions
- GitHub Pages
- FormSubmit 静态表单邮件转发

全站正文使用 EB Garamond 与中文宋体回退栈混排：英文、数字和罗马编号通过 Google Fonts 官方样式加载 EB Garamond 400 / 500，中文优先使用本机 Noto Serif SC、思源宋体、Songti SC、STSong 或 SimSun。首页英文品牌字标单独使用 Google Fonts 的 Beau Rivage 手写体，并以 Cormorant Garamond 和 EB Garamond 回退；首页电话接听后的核心文案与关于页静态求职方向仍使用作者特里王发布的免费商用“朝华打字机”字体。加载提示、首页作品方向、明信片操作提示及 CCD 屏幕使用完整宋体栈，避免本地字符子集缺字导致同一句混入不同字体。关于页明信片背面的正文和收寄信息使用墨豹堂发布的免费商用“漓雨手书”；优势页夹板内的眉题、标题与正文使用特里王发布的免费商用“迫真打字油印体”。优势页塔罗文字层统一优先使用“微雅软黑”，并以 Microsoft YaHei、PingFang SC 与 Noto Sans SC 作为跨平台回退；仓库不内嵌未获授权的微雅软黑字库文件。三款已本地化字体均以按实际文案生成的 WOFF2 子集嵌入，来源与授权说明保存在字体目录。项目主体仍为纯前端静态网站，不包含自建后端、数据库、登录或支付；联系表单会把访客主动填写的姓名、回复方式、来意和留言提交给 FormSubmit，再转发至站点公开邮箱。

## 4. 运行、构建与发布

安装依赖：

```powershell
npm.cmd install
```

本地开发：

```powershell
npm.cmd run dev -- --port 5174 --strictPort
```

也可直接双击根目录的 `启动本地预览.bat`：首次运行会安装依赖，随后在独立的最小化命令窗口中启动本地开发服务器；启动器会等待作品集页面真实返回成功后再打开 `#home`，并在 `5174` 被其他程序占用时给出明确提示，避免只凭端口监听状态误判预览可用。需要停止服务时，打开最小化的 `Portfolio - Local Preview Server` 窗口并按 `Ctrl+C`，或直接关闭该窗口。

本地预览路径：

```text
http://127.0.0.1:5174/zhechun333/#home
```

生产构建：

```powershell
npm.cmd run build
```

构建产物位于 `dist/`，不手动维护、不提交 Git。`vite.config.js` 使用 `base: './'`，使资源 URL 相对于当前 HTML 页面生成，同时兼容 GitHub Pages 项目子路径和自定义域名根路径。React 中的公开素材通过 `import.meta.env.BASE_URL` 生成地址，CSS 中的素材使用可由 Vite 构建转换的相对路径。

`.github/workflows/deploy-pages.yml` 在 `main` 分支更新后执行：

1. 检出源码。
2. 安装 Node.js 和 npm 依赖。
3. 执行生产构建。
4. 上传 `dist/`。
5. 发布 GitHub Pages。

## 5. 页面结构

### 5.0 作品集加载层

- 加载层直接复用经历页的浅绿纸面底纹与纵向柔和渐变；页面上方的三只蝴蝶统一采用半透明珠光白薄翼质感和右上航向，各自以错开的四帧循环自然拍翼，并分别读取同一加载进度上的三个相邻飞行点：轨迹从左侧快速爬升，在上方缓慢越过峰顶后向右下沉，再于右侧重新抬升。三只蝴蝶在主曲线附近保留不同的上下偏移，使峰顶平台段也维持清晰的前后、高低错落；每只蝴蝶附带克制的暖白呼吸光、短尾迹、十字星与两颗错相流萤，光效随蝴蝶移动但不铺满页面。单只基础尺寸锁定为加载黑猫容器的一半；手机端将飞行舞台放在顶部安全区下方，并略增蝶翼尺寸与深色轮廓投影，保证浅绿背景上的辨识度。框外加载引导使用完整宋体，并与关于页拍立得引导词共用浅绿色文字；宽幅磨砂框以更清透的玻璃底承载小猫、2px 进度横线与线下百分比。
- 蝴蝶群位置与淡出、小猫位置、横线填充和 `00%—100%` 数字读取同一进度状态；加载层会等待首页关键贴纸、电话、唱片机、完整字体与浏览器绘制帧，未完成时最高停在 92%，全部就绪后才释放最后 8% 并进入首页 `#home`。
- 五个一级章节各自维护关键图片资源清单；当前章节稳定显示后，浏览器在空闲时预加载顺序中的下一章节，并缓存图片解码结果与关于页 Three.js 模块。若用户在预加载完成前快速导航，当前完整页面会继续保留，目标章节就绪后才执行切换，避免出现图片后补、字体跳变或半成品首帧。
- 加载期间锁定页面滚动并在卸载时清理动画帧、计时器和事件监听；Escape 仅在首页资源已经就绪后允许快速结束退场，`prefers-reduced-motion` 下改用静态猫、固定翼位与静态低亮光点，保留足够辨认加载画面的最短展示时间，但不绕过内容就绪门槛。

### 5.1 首屏 `#home`

- 使用低饱和自然系开放式封面构图，中央偏左放置用户提供的薄荷蓝复古转盘电话。电话保持固定位置，不提供拖动；桌面端将电话、接听文案和状态提示作为一个叙事组整体下移约两行并向左微移 32px，给上方信息留出更清晰的层级，同时平衡右侧唱片机与贴纸的视觉重量。首次点击后以克制的左右摇摆配合 Web Audio API 合成的双音脉冲铃声模拟来电，再次点击视为接听，立即停铃，并在电话听筒正上方的稳定留白中使用“朝华打字机”细明体逐字显示“用审美判断、内容组织和平台语感，完成从选题到传播反馈的运营表达。”；该字体只作用于这段核心文案，显现过程不会推动电话位置。电话状态文字以电话可见机身为基准居中置于机身下方。电话体验层的透明留白不接收鼠标事件，仅电话本体接收点击，避免遮挡右侧可拖动吉他。铃声只在用户主动点击后开始，离开页面时会释放音频节点；低动态模式停用摇摆和逐字等待，保证内容直接可读。
- 首页不展示森林主图、裁切图或其他照片素材，并移除封面框、雨滴、柔光、闪光、颗粒和指针跟随等背景动效。
- 主标题为英文 `zhechun`；杯身字环单独使用紧凑大写 `ZHECHUN` 字面，两组连续单词以正向朝上的独立字母组成青提气泡饮杯身偏上的透视“杯腰字环”，整体相对杯子上移约一行字高，沿桌面 `49×12px`、移动端 `38×10px` 的扁椭圆持续向左单向循环。字环采用俯视透视：椭圆下半圈为靠近观看者的前景，字面完整、清晰且略微放大并覆盖在杯身前；上半圈为远离观看者的后景，字面缩小淡化并被杯身遮挡，左右两侧横向压窄。一圈固定为 7 秒线性匀速，接缝位于同一几何点，不折返、不变速也不抖动。字标与杯子组成同一个可拖动拼贴，支持鼠标、触摸、方向键、Shift 加速以及双击、Home 或 Escape 复位；低动态模式停止环绕并保留完整静态字环。首页不公开真实姓名，岗位方向与能力说明位于首页首屏画布左上方、青提杯正上方并与杯顶保留明确间距，随首屏内容正常滚动离场，不挂载在固定页头；移动端与导航底边保留安全间距，字体、灰薄荷色、字号、字距和行高与电话操作引导词保持一致。
- 首页非电话贴纸以青提杯的可见高度建立基础比例，不约束各素材宽度；杯子与唱片机共用上基线，人物贴纸、薄荷吉他和猫咪共用下基线，其中左下人物贴纸以及右下两张贴纸在原统一尺度上整体放大 1.3 倍。薄荷吉他与猫咪只在默认构图中相邻，各自拥有独立拖拽、旋转、键盘移动和复位状态，可以被访客分别移动。移动端沿用同一四点结构与这组 1.3 倍关系，方向说明位于上排贴纸之间，主操作承接在右下贴纸下方。
- 主要入口为“精选项目”和“联系我”。
- 首页音乐入口使用 `imagegen` 依据用户两张参考图重新生成的真实俯视唱片机透明贴纸作为机身底层：造型严格参考图一的低矮方形机身、占据主体的大唱盘与右上轴座布局，色调严格参考图二的雾鼠尾草绿、半透明浅绿、乳白和柔和灰。网页在机身上叠加半透明浅绿唱片、深灰长唱臂和指示灯；三枚乳白控制键位于机身下沿，控制区与机身同宽并采用三等分网格，按钮、内圆和图标会随唱片机等比例缩放且保持完全一致，播放态及键盘焦点只以内描边反馈，不改变按钮外部占位。桌面端曲名与播放状态位于按钮下方，移动端视觉隐藏文字但保留 `aria-live` 播报。默认曲目为 Mixkit 授权的《继续走》（Just Keep Walking / Michael Ramir C.），使用 HTMLAudioElement 实现加载、暂停续播、循环、失败反馈与切歌；另保留四首 Web Audio 实时合成氛围曲。播放时唱片恒速旋转、唱臂落下、指示灯点亮，暂停时依次复位。移动端在用户按下控制区时同步创建并解锁 Web Audio 上下文，避免合成曲在 iOS 上错过用户手势授权。唱片机仍可拖动和键盘移动，控制键区域不会触发拖拽；双击机身、按 Home 或 Escape 可复位。
- 音乐播放器挂载在五个一级页面共用的站点层级：唱片机控制界面只在首页显示，但开始播放后切换关于、经历、优势或联系页不会卸载音频、重置曲目或丢失播放状态；返回首页后继续显示当前状态，只有用户主动暂停、切歌或刷新站点才结束当前播放。
- 首页默认构图采用上、下两条共享基线：左上青提杯与右上唱片机形成第一组平衡，唱片机默认上移约 `42px`，让其顶边与左侧作品方向文字处于同一横向基线，左右位置和自身朝向保持不变；左下人物、右下薄荷吉他与猫咪形成第二组平衡，电话叙事组占据中央留白。桌面端唱片机与右下小猫共用居中内容画布的横向中心列，不随超宽视口贴向浏览器右缘；双击、Home 或 Escape 会回到新的默认位置。杯子、人物、薄荷吉他和猫咪均支持鼠标、触摸、方向键和旋转把手交互，后两者只在初始位置相邻、不组成拖拽组；唱片机继续独立拖动。低动态模式关闭非必要过渡，所有交互对象保留焦点反馈且不遮挡主操作与页底续滑入口。

### 5.2 关于与履历 `#about`

- 拍立得照片背面的“求职方向”标题、中文方向与英文说明全部使用常规字重，关闭浏览器伪粗体，不使用粗体语义标签。

- 个人照片：`public/media/pdf-images/full_p02_i01_Im73.webp`。
- 关于页顶部使用用户指定的奶油黄色 Mini 8 拍立得构成两段点击叙事。相机贴纸使用透明边缘过渡的发布素材，去除外轮廓、下沿残留的浅色底边和旧版双栏人物面板遗留的竖向分隔线；初始只显示透明背景的相机前景。点击相机后合成短促的高频快门帘幕声与低频机身闭合声，保持为明确的“咔嚓”而不再叠加持续马达和滚轮声；个人照片始终位于相机后方，从机身下沿向下送出，并由浅白低对比逐渐完成显影，完成后停在接近出片口的位置。照片完成后开放点击但不改变与相机的前后层级，点击照片会进行卡片翻面；暖米色背面将“求职方向”作为大号昭华打字机字体标题，其下用更小的同字体正文显示“内容策划 · 账号运营 · 品牌表达”，继续点击可翻回正面。相机与照片整体高于当前页面内容、外层保持透明；移动端等比收敛，低动态模式保留点击因果与声音但跳过长位移、抖动和翻面等待。
- 初始“点击奶油黄色拍立得，拍出我的照片”提示固定在相机机身下方约 20px，快门瞬间保留短暂反馈；照片开始出片时提示自动淡出，不再显示“照片已完成”等后续状态文字，避免干扰照片与下方核心竞争力段落。
- 原先位于拍立得上方的核心竞争力段落已改为一张可翻面的异形明信片：常态小卡左对齐展示浅蓝色大象与粉色章鱼正面，桌面端宽 220px、手机端宽 190px；点击后沿纵轴翻到真实背面，并进入页面最外层的居中阅读层，在约 920ms 内由原位置方向慢慢放大至最高 650px，同时以 15px 背景虚化聚焦背面留言。点击卡片、背景或按 Escape 可收回。正反面 PNG 均已按实际 Alpha 边界裁去透明画布，翻转面不再使用矩形裁切。背面左栏正文按约 13 个汉字自然分行，保持正常字宽，并在文字上方增加独立横线，不再通过横向缩放挤入纸面；右栏以粉色章鱼邮票、椭圆邮戳和“作品与经历 / 七段履历”的收寄件信息填满邮票框与地址线，保持真实明信片的左右信息平衡。页面严格维持“真实雨伞最大、拍立得其次、明信片最小”的常态比例；交互支持键盘、清晰焦点和低动态即时切换；拍立得、出片、显影和照片翻面内容保持不变。
- 明信片背面左栏正文重新平衡为五行，每行文字盒底边与底图五条等距横线逐一对齐；正文、邮票文字与右栏收寄信息统一使用常规字重并关闭伪粗体，手写内容使用本地“漓雨手书” WOFF2 子集，保持正常字距，并按卡片容器宽度同比例缩放，保证常态小卡与放大阅读层保持同一排版关系。手机放大阅读层的右栏收寄信息会随底图四条逐行缩短的地址线分别控制宽度并整体略向左收，中文继续使用手写字体，两行英文使用现有衬线字体的紧凑字形，确保文字与明信片斜边之间保留可见安全距离。
- 纸张内不再保留“运营 / 策划 / 产品”切换与重复能力描述，避免与下方核心竞争力段落形成多重重复表达。
- 邮箱入口不再占用打字机叙事区域，统一由页头“联系我”和联系页承接，保证初始画面只保留照片纸与机身。
- 关于页不再保留“关于与经历 / 偏内容创意型的新媒体运营”顶部标题区，也不重复平台说明与人物自述。打字机后只保留一段核心竞争力表达：从产品卖点与用户情绪提炼内容切口，按平台角色组织种草、深度表达与互动承接，并将选题、生产、反馈和转化线索组织为可执行、可复盘的传播链路。
- 关于页在所有尺寸都采用“核心竞争力 → 拍立得人物叙事 → 真实伞下履历”的纵向顺序；核心段落以受控行长独立出现，交互履历继续在拍立得下方展开。完整七段经历列表及卡片阅读层统一使用常规字重，“职责 / 成果”标签与对应正文保持同一字号和行高，通过颜色与位置建立层级，避免局部忽粗忽细。
- 伞下履历使用 Three.js 参数化建模：八片独立高拱透明篷面采用轻微珠光的薄膜材质，外沿由八段自然下垂的波浪包边组成，不再使用生硬的整圈圆环。每根主伞骨由纤细的内骨段、乳白关节球和外骨段组成，不再表现为一根完整光滑弯管；半透明内撑、极细面料接缝、白色骨尖球、锥形伞帽、纤细金属中轴和连续奶白色 J 型把手共同保持轻盈的透明长柄伞体积。中轴以伞顶为固定端缩短四分之一，把手同步上移，保证轴杆与把手仍连续衔接。开合不再缩放整组模型：八片篷面、分段主骨、八段伞沿及面料接缝分别保存展开与闭合顶点，并通过 GPU morph target 从开放拱面折叠为贴近中轴的竖直长锥；闭合态会让篷布、骨架末端、关节、内撑滑套和履历悬点沿中轴明显向下延长，符合长柄伞收拢后的纵向结构，同时中轴和把手始终保持竖直。七张履历卡使用带厚度的 BoxGeometry，正反面信息由 CanvasTexture 绘制；七个悬线固定端直接读取各自分段伞骨的骨尖坐标，并在开合过程中与骨尖使用同一组几何插值，因此线头始终与伞骨连续，不再停在伞沿下方的独立半径上。不同悬线长度继续错开前后卡片，每根悬线与卡片组成独立高阻尼摆锤：只有旋转、拖拽、开合与卡片碰撞会产生克制的径向外甩、切向拖后和停止惯性，静止后快速回到接近竖直的稳定姿态，不再叠加持续底风、随机阵风或局部扰流。卡片相对悬线只保留很轻的反向扭转，既能说明“伞先走、负载落后”的因果，也不会长期歪斜影响阅读。每张卡片另有贴合实际厚度、宽高和当前旋转姿态的 Three.js OBB 定向碰撞盒，七张卡片每帧最多进行 21 次两两检测；展开和开合过程中检测到穿模后会把分离冲量反馈到两侧摆锤速度，让悬线带着卡片自然弹开。收伞末段停止碰撞冲量；闭合悬点随骨尖收紧，卡片外张角同步降低，因此七张原尺寸卡片会围绕中轴形成紧凑错层，而不是悬空散开。长短悬线保留不同响应节奏，旋转时同时呈现均匀分布、近大远小、侧面厚度、前后遮挡与背面内容。碰撞与摆锤反馈复用现有 Three.js 动画循环，不引入完整刚体引擎或新的外部依赖；低动态模式直接使用稳定开合姿态并停用动态碰撞反馈。收伞时卡片几何体、厚度、文字纹理和材质透明度始终保持不变，不随伞篷压缩或淡出；只有用户选择某张卡片并进入阅读层时，该卡片才执行下落淡出。默认展开并保留收起、再次展开、旋转 / 暂停、射线点击卡片飘落和阅读交互；选择卡片后会打开一整张居中的放大明信片，清晰展示编号、时间、职位、公司，以及独立成行的职责与成果。雨伞下方的完整七段经历列表同样读取这组统一字段，每段不再在“职责”和“成果”之间二选一。两枚控制按钮采用接近旧版 iOS 锁屏数字键的亚克力半透软磨砂胶囊：低透明度雾化底色、柔和内高光、淡薄荷色晕染与微弱扩散光共同形成无硬阴影的毛玻璃层次，并保留清晰焦点和按压反馈。移动端只后移透视相机以扩大横向取景范围，完整保留左右伞沿和安全留白，不压缩伞体比例、履历卡尺寸或相对位置；桌面镜头保持原构图。渲染器使用 ACES 色调映射、响应容器尺寸并限制像素比，组件卸载时完整清理动画帧、监听器、几何体、材质与纹理；低动态模式停用循环旋转、摆动和阅读层入场动画，但保留完整内容与操作。
- 雨伞开合与旋转控制使用无底色透明磨玻璃：仅保留白色折射边、内高光、背景模糊和轻微空间阴影，悬停及选中态也不填充色块；按钮组与下方操作说明固定保留 16px 阅读间距。本条取代上文旧版按钮的薄荷晕染描述。
- 雨伞镜头距离按画布宽高比同时校验伞体纵向高度与伞沿横向宽度，桌面端和不同窄屏都完整保留伞顶、左右伞沿及稳定安全留白；手机端舞台不再使用负外边距，伞体、履历卡及其相对位置不做非等比缩放。Three.js 清屏、场景、舞台和画布均显式保持透明，使页面原背景连续穿透模型区域。
- 精选项目总览初始只显示居中的竖放透明富士 Superia 200 胶卷；移动端胶卷可见长度与上方 CCD 的可见高度接近，不再为容纳胶片而额外缩小。胶片及其项目按钮在视觉、鼠标和键盘访问中全部收起，“点击胶卷 / 拉出项目胶片”提示与胶卷图片底边保持约 18px 间距，作为舞台固定前景独立排在胶卷下方，不覆盖罐身或底座，也不随胶卷的点击反馈或拖拽轨道位移。点击后胶卷受力移向舞台左侧，唯一的黑色 35mm 项目胶片从右侧狭窄出片缝连续水平拉出；定位以胶卷罐体的可见边缘为准，不使用透明图片盒底边，胶片从罐体侧面中部伸出。移动端仅向上扩展片基及上排齿孔，下边缘、项目画幅与胶卷位置保持不变。常规移动端保留与桌面一致的 `248×202px` 项目画幅，更窄屏幕才适度收敛，避免项目图和文字过度拥挤。片基包含上下连续齿孔、画幅间隔、暖色边缘透光、斜向柔光与低强度画幅反射，形成克制的真实胶片材质且不影响文字清晰度；展开过程由颗粒摩擦、齿孔节拍和末端卡位三层 Web Audio 合成声同步承接，完成后才开放横向拖动、惯性浏览和项目点击。连续胶片承载十二个项目，每个项目只显示一张主图并占用一个独立画幅，项目编号、名称、类型与首项成果叠入对应画幅；向左拖动时胶卷随轨道退出并被舞台边界裁掉，拖回最右端时重新出现。低动态模式保留点击与声音因果，但将位移和拉出等待缩短为即时切换。项目页移除重复的 Project Field / 项目总览标题行，CCD 与导航距离收紧。CCD 默认在机身下方显示与机身左边缘对齐的模式转盘提示，点击后屏幕亮起并播放一次合成快门声。胶片舞台按视口横向延伸且页面背景直接穿透，下方项目汇总仍沿用正文栏宽度。

- 胶卷展开后，现有 35mm 项目胶片的上边缘与上排齿孔向上扩展到胶卷上沿，项目画幅同步向上填满上下齿孔之间，形成与真实胶卷参考一致的连续出片关系；画幅底端、下排齿孔、胶片底边及胶卷位置保持不变，不再使用独立悬空黑色片带。

### 5.3 经历总览与案例窗口 `#projects`

当前以“8 个运营核心案例 + 4 个 AI 数字作品”的双层结构呈现：运营案例证明主岗位能力，AI 项目作为独立数字作品展示，不与内容运营成果混为同一职业权重。

1. 品牌自媒体运营
2. 小红书种草协同
3. 教育类小红书获客
4. B 端软件内容运营
5. 旅游攻略内容引流
6. 热点传播与官方账号
7. 个人账号运营
8. 内容写作与补充作品
9. 不牛马厨房 AI 产品实践（补充能力）
10. Hatch Wheel Pet（转盘动态宠物制作 Skill）
11. Prepare and Review Interviews（面试前后证据链 Skill）
12. Make Transparent Sticker（透明贴纸标准化 Skill）

三个 Skill 案例均在主视觉下方直接标注对应 Skill 名称。Hatch Wheel Pet 使用“慵懒小猫”四状态素材展示，不设置在线体验入口；Make Transparent Sticker 展示人物、猫咪、饮品和乐器 4 类素材，人物贴纸只作为主图出现一次，图库不再重复收录。

“小红书种草协同”案例以产品、用户与博主三端判断为核心：结合个人博主与品牌方双侧经验，除展示 84 篇内容协同、场景匹配和标签统一外，也说明通过产品置换、博主转介绍完成 2 次中尾部合作，并沉淀外部资源。对外不写未确认的固定市场报价或具体降本金额。

章节标题使用一台冷调银色复古 CCD 成品图：相机外部背景已抠为透明，银色机身保留真实高光、凹槽、边缘阴影和金属控制区，黑色屏框与模式转盘不变；网页在原白屏位置覆盖同尺寸文字层，使用关于页明信片背面的“漓雨手书”字体展示 `Selected Work / 项目经历 / 从内容策划、平台运营到转化承接，以代表项目呈现执行过程与结果。`，并随 CCD 容器等比缩放。图片替代文本继续完整保留辅助阅读说明。CCD 桌面端宽度为 256–304px，窄屏端使用内容区宽度的 80%；机身、屏幕文字层、遮罩、模式转盘命中区和下方提示作为同一容器等比缩放。连续胶片保留多项目集合的视觉角色，舞台按视口延伸并在两侧保留约两行正文的自适应留白，透明外层不再形成独立底板；容器高度贴合胶片本体，不保留额外空白。CCD 操作提示与胶片之间采用和关于页一致的纵向节奏：桌面端 72px、窄屏端 40px。所有视口均保持 CCD 原始宽高比，不拉伸机身或屏幕内容。初始进入经历页时，屏幕由黑色遮罩覆盖；点击相机右上模式转盘的透明命中区后，以约 1 秒的 LCD 亮屏渐隐逐渐显出白屏和标题，按钮支持键盘焦点、`aria-pressed` 与低动态即时显示。十二个项目统一进入一个可拖拽、带惯性且不会劫持页面滚轮的项目场域；胶卷先展示八个运营项目，再依次展示不牛马厨房与三个 AI Skill 项目。场域只使用真实项目，不复制内容或显示随机占位，每张项目图版展示编号、项目名、类型和首项成果，点击可直接打开原有案例阅读窗口；Hatch Wheel Pet 的胶片封面使用不牛马厨房粉色小厨娘视觉，并以白色画幅和独立居中裁切突出人物，详情页仍保留原有四状态宠物素材。场域下方始终提供“查看全部项目汇总”入口，展开后按 01—12 显示项目名、类型和首项成果，作为无需理解拖拽操作的直接阅读路径；手机端十二项保持单行横向胶卷，组件明确限制为横轴拖动并通过 `pan-y` 保留页面纵向滚动，汇总入口与列表使用完整单列信息结构。从画布或汇总列表关闭案例时，焦点都会返回刚才点击的项目入口。案例窗口内的截图轮播保留相邻图片预览、缩略图、触摸横滑和灯箱；切换下一张时当前图向左退出、下一张从右进入，切换上一张则反向，自动播放和缩略图跳转同样保持方向连续。低动态模式停用拖拽惯性、轮播位移与非必要过渡。项目图片和文字在 `src/App.jsx` 的项目数据中维护。

- CCD 下方左侧设有可点击和键盘操作的真实白色折纸飞机透明素材，主翼、中央折脊、回折翼面和灰白自阴影共同形成清晰立体结构；飞机机头常态朝右，桌面约 `168×124px`、移动端约 `144×108px`，按当前留白尺寸动态生成“左下缓升圆顶—中段宽缓下沉—右侧平滑抬升”的三段长弧轨迹，以 8.2 秒线性速度飞行并停留在终点；重复操作可重新放飞，窄屏会重算路径，低动态模式直接抵达。
- 项目胶片下方的“查看运营核心案例与 AI 实验室”汇总入口向上收紧约两个正文行距；标题在桌面端按内容宽度分配并保持单行，窄屏通过响应式字号保证完整显示。
- 优势页夹板、工作方法钢琴、核心能力邮票与塔罗以关于页明信片、拍立得及经历页 CCD 的可见物件量级为共同基准等比例编排：桌面端依次约为 `280–340px / 680px / 224–256px / 280px`，移动端依次约为 `300px / 330px / 232px / 224px`；夹板内眉题、主标题和说明文字随成品同步缩放，并保持在毛边白纸安全区内完整显示。
- 优势页工作方法钢琴的每块白键以完整网格轨道作为统一点击热区，包含键面顶部原先由旧固定行高留下的白色空带；鼠标与触控在任意白色区域按下时都会立即播放对应音阶、让整块白色键面统一下沉并生成上飘音符，避免局部无反馈或横向透视造成的按压错觉，键盘通过 Enter / 空格独立触发。低动态模式保留整键压暗反馈但取消位移。钢琴下方的 `01–06` 工作方法汇总复用塔罗下方内容汇总的折叠形式，默认收起，点击标题行展开六步全貌，再次点击收纳；按钮保留圆形箭头、键盘操作与状态语义，展开内容在桌面和移动端均使用连续单列链路。

### 5.4 优势 `#strengths`

- 六步流程：内容判断、选题策划、内容制作、发布运营、互动承接、数据复盘。
- 核心能力：复杂产品的平台化表达、从内容测试到线索承接、品牌／用户／创作者三端协同、资源受限下的快速验证。
- 工作方法钢琴与核心能力塔罗之间设置一枚蓝色西高地犬翻转邮票，初始显示文字背面，以本地化的迫真打字油印体展示“02 / 核心能力 / 能独立推进，也能进入协作”，让访客先理解后续章节；点击或键盘激活后沿纵轴翻到带西高地犬与复古电话图案的正面，再次激活返回文字面。邮票不再居中，改为相对所在内容列左边缘保留约三个汉字宽度后左对齐。正反两面均按齿孔外缘裁成保留透明通道的高质量 WebP，不保留矩形底色；桌面端显示宽度为 `224–256px`，移动端使用 `min(62vw, 232px)`，正反面、HTML 文字、编号圆章、间距与交互命中区均按上一版比例同步扩大两倍。组件支持清晰焦点、状态化无障碍标签、移动端等比缩放和低动态即时切换。
- 章节开场使用从用户参考图中抠出的浅蓝色金属夹板与毛边白纸，外围白底、账号标签和下载提示均已移除；夹板保持原宽高比，桌面宽度控制在 `280–340px`，窄屏以 `300px` 和内容区安全宽度中的较小值封顶。迫真打字油印体文案随夹板等比例收敛，文字盒约束在毛边白纸安全区内；正文保持为单一连续句，只按纸面宽度自然换行。
- 优势页的六步“工作方法”直接组成一架竖向黑白钢琴：左侧黑色琴身使用去除平板外框、摄像头、扬声器网格和底部硬件后的 `strengths-piano-keyboard-v2.webp`，仅保留连续黑色琴身与白色音符；右侧七个连续白键构成完整琴键，`Do` 键承载“工作方法 / 一条完整的内容运营链路”标题，内容判断、选题策划、内容制作、发布运营、互动承接和数据复盘依次顺延至 `Re—Xi`，七键均有明确内容且不虚构额外工作步骤，黑键从琴身伸入白键区域。所有标题、键面内容和音名统一使用常规细明体与 400 字重，文字在黑键末端之后略向左收紧；中等及窄屏将标题与说明上下排列，禁止省略号截断。操作引导复用关于页拍立得提示的字体、字号和浅绿色。点击或键盘激活任一白键会产生按压反馈，通过 Web Audio 合成 `Do—Xi` 七个音，并从对应琴键高度生成一个清晰的半透明白色音符向黑色琴身上方飘动淡出；粒子层严格裁切在钢琴主体内，不应用到优势页其他内容。五条核心能力位于钢琴下方，以带编号邮票、邮戳、书写横线和漓雨手书的小明信片作为五个 3D 圆环面；圆环自动缓慢旋转，支持鼠标与触摸横向拖动、惯性、左右方向键及下方“一—五”圆形序号按钮直接切换，当前序号与正面明信片同步。卡片按手机、中间宽度和桌面三档重新计算宽高与圆环半径，并以最长正文完整显示为尺寸下限；低动态模式停止自动旋转但保留拖动与序号切换。
- 琴键点击或键盘激活后，半透明白色音符从对应琴键的文字栏上方垂直飘升淡出；不再使用随机横向漂移或固定左移，声音、按压反馈和低动态降级保持不变。
- `v04.26` 起，核心能力圆环中的五张明信片已替换为五组原创猫咪塔罗牌正反面：全部卡面统一为 `750 × 1200`、`5:8` 的实体塔罗比例，并使用 CSS 反射层呈现克制纸面光泽。`v04.28` 按第四张完整竖版牌面的安全区补全第一张橘白猫与第五张白猫，四边外框、上下装饰、猫身与象征物均完整保留，不再通过满版放大裁切破坏构图；`v04.36` 将第三张重绘为银白猫与清透冷粉蓝星盘花饰，加入月相、星座连线、云纹、牡丹和四角星徽，提高整套牌面的精细度。文字面图片只保留无字卡框，编号、英文眉题、标题和正文均由 `src/App.jsx` 中可随时修改的 HTML 数据叠加，不把文字烘焙进图片；五张牌统一使用微雅软黑字体栈、常规 `400` 字重并关闭伪粗体，标题只通过字号建立层级。普通卡文字层四边保留 `2em`、约两个汉字的安全距离；第五张黄色卡框的左右装饰竖线位于该通用安全区内，因此单独使用上下 `2em`、左右 `4em`，让标题和正文与竖线保持与其他卡片边缘一致的内距。首次进入优势页时五张牌以图案面叠成待发牌堆，点击后展开为圆环并开始旋转；点击牌背或使用 Enter / 空格会将该牌居中翻到文字面，阅读期间暂停，翻回图案面后继续旋转。使用方向键、编号或汇总切换能力时，会先将已阅读的牌恢复为图案面；拖拽、惯性和低动态操作继续保留。下方“一—五”当前编号复用关于页伞下控制按钮的透明磨玻璃选中态，以冷白折射边、内高光和轻微环境阴影取代黄色填充。塔罗圆环下方另设可折叠的五项核心能力汇总，沿用经历页汇总结构；展开后可扫描完整标题与说明，点击任一汇总行会将对应塔罗牌转至中间。
- AI 工作流与数字作品以独立能力明信片呈现，说明其用于补充内容运营的数字化落地能力，不包装为专业开发经历，也不使用替面试官预设评价的标签。

### 5.5 联系 `#contact`

- 作为整页收尾，保留求职表达、邮件联系入口和可直接提交的联系表单。
- 联系页直接透出全站浅绿色纸面背景，不使用独立底色或遮罩；内容区左上角保留猫咪凝望悬挂金鱼的方形窗景图片，该图片与章节预加载清单统一通过 `assetUrl()` 生成公开地址，并使用优先加载与异步解码，兼容本地子路径和正式部署路径。四条大小与高低错落的透明金鱼集中固定在联系表单右半侧上沿，不再从图片中飞出。四条鱼均可通过点击或键盘独立激活，并分别吐出 `2 / 5 / 3 / 1` 个较大的乳白、淡金与浅蓝透明泡泡；每条鱼使用略有差异的扩散幅度，泡泡向左上方自然分散上浮并渐隐，可重复触发且不遮挡表单操作。低动态模式只短暂显现泡泡，不执行长距离漂移。三段收尾标题紧接在图片下方，邮件入口和返回首页操作保持无框布局，并使用适合浅色背景的深绿色文字与分隔线。
- 排版保留章节页码与三段大标题；联系表单整体居中，并复用伞下控制键的清透亚克力玻璃语言，通过白色折射边、内高光、背景模糊和克制扩散阴影统一来意选项、输入框、发送按钮与邮箱入口。作品集建议、能力建议、合作邀约和面试邀约采用可独立多选的玻璃按键，选中后呈接近乳白的浅鹅黄色，并短暂扩散一层淡微光，再次点击即可取消；提交时会将全部已选来意合并进邮件。访客仍需填写称呼、任意一种回复方式与具体留言；两段联系引导词位于表单与邮箱入口之后的最底部。输入框使用保持可见语义的浮动标签，提交过程提供发送中、成功和失败反馈，并保留邮箱入口作为降级方式。
- 表单通过 FormSubmit 的跨域 AJAX 接口转发到公开邮箱，不在源码中存放 SMTP 凭据；服务确认提交成功后，发送键切换为成功文字，纸飞机从按钮上方向右上方掠过，并播放项目内置的克制起飞与到达提示音。低动态模式取消飞行位移但保留成功文字和声音；首次上线需由邮箱收件人完成 FormSubmit 激活，后续邮件才会正常转发。
- 联系页三段收尾标题统一使用“微雅软黑”字体栈与常规 `400` 字重，关闭伪粗体，以更克制的软黑字形衔接窗景和玻璃表单；该字体只作用于三行标题，正文与表单字体不受影响。仓库不内嵌未获授权的微雅软黑字库文件，并以 Microsoft YaHei、PingFang SC 与 Noto Sans SC 作为跨平台回退。移动端三段标题各自保持完整单行，不拆分末尾文字。
- 不展示手机号和微信号。

补充：第 09 项的 AI 表述仅用于说明辅助制作流程，用户侧功能为餐食决策、随机转盘和偏好筛选；桌面端海报与叙事采用均衡双栏。第 10—12 项分别展示动态宠物资产生产、面试前后证据链和透明贴纸标准化三种 Skill 实践，其中面试案例只使用匿名化结构与内容，不公开候选人或公司身份。普通案例的原始作品链接作为主图与项目摘要之后的独立全宽索引区呈现，不挤占右侧摘要，也不占用底部折叠区域。

## 6. 已实现功能

### 导航与页面反馈

- 桌面端固定完整导航，首页右侧在“联系我”前提供由圆形图标展开为圆角输入栏的项目搜索；761px 以上视口始终预留其完整展开宽度，输入、结果和导航保持稳定，查询或结果获得焦点时输入栏不会意外收起。移动端将“首页 / 关于 / 经历 / 优势 / 联系”以紧凑居中组排在品牌与搜索图标之间的同一行，不使用下拉菜单；菜单文字使用微软雅黑字体栈，当前项采用半透明冷调雾灰玻璃，以细冷白高光边、柔和扩散内阴影和低对比环境投影建立克制悬浮感，按下时轻微下沉且保留清晰焦点。点击搜索图标后，项目关键词输入区会作为独立的玻璃前景层悬浮在顶栏下方，使用半透明折射、内高光和景深投影，并自动聚焦；展开不会改变顶栏高度或向下推挤页面正文。关键词匹配名称、类型、摘要或核心数据后，可从任意视图直接打开对应案例；从经历页再次搜索同样有效。点击导航项后只展示对应的独立内容视图，不再把五个模块连续铺成长页。
- 当前视图由 URL Hash 保存，可直接访问、刷新保留，并支持浏览器前进与后退；导航使用 `aria-current="page"` 标识当前页。
- 顶部菜单与浏览器前进后退切换视图时内容轻量显现；五个视图（包括首页）共用页底续滑轨道，续滑换页时目标章节同步挂载并在浏览器绘制前直接位于 `scrollY: 0`，随后以约 440ms 的轻微上移与缩放弹入建立前后章节的空间连续性；只阻止实际触发换页的那一次滚轮或触摸事件，后续连续输入会立即用于正常浏览新章节。低动态模式关闭这段位移与缩放。
- 独立页统一使用超大低透明度英文水印、英文眉题、中文正式标题、细分隔线和纵向留白组织章节；关于、经历、优势和联系保持各自原有内容顺序。
- 当前导航除文字状态外，在菜单项下方显示约 5px 小圆点，并继续保留 `aria-current="page"`。
- 五个独立视图均提供居中的连续浏览入口：首页→关于→经历→优势→联系，联系页提供返回首页。页底只预留约 96–124px 的纯背景轻拉轨道，并为短页面同步增加包含轨道的最小总高度；首次下拉达到约 34px 时，无需停止输入便会立即触发自然归位并显示下一章节提示，归位期间的继续下滑不会与动画互相拉扯。提示使用常规 React 状态更新，不在挂载阶段强制同步刷新。提示出现并完成短暂解锁后，再次向下滚动累计约 44px 或触摸上滑约 25px 即可切换章节。归位由当前输入速度驱动：输入越快，触发后继续向下的惯性距离越长，但最多只额外延伸约 30px；到达惯性最低点后先稳定实际滚动位置，再以浏览器合成层的亚像素位移在约 460–720ms 内将页面内容柔和归位，避免短距离逐帧滚动的阶梯感。无边框文字与箭头从底部小幅上移并缓慢渐显；完成入场后两者共用 2.8 秒呼吸周期，箭头向下约 6px时文字同步轻微缩小到 0.99 并降低至约 0.8 透明度，箭头回到上方时文字恢复完整清晰。向上滚动会立即取消待执行归位。普通页面提示使用低饱和浅苔绿，联系页使用浅鼠尾草绿；低动态模式关闭呼吸并即时归位。
- 导航底部显示当前视图的页内阅读进度。
- 离开首屏后显示返回顶部按钮。
- 锚点使用平滑滚动。
- 经历、项目、优势和联系内容首次进入视口时自然显现。

### 项目轮播与灯箱

- 当前选中的项目包含当前主图、前后预览、左右切换和缩略图条；手机端可在主图区域左滑查看下一张、右滑返回上一张，拖动过程提供跟手位移且不阻断纵向滚动；未选项目只加载紧凑索引。
- 案例阅读窗口打开期间，所有项目详情在内容超过窗口高度时均可在窗口内部纵向滚动，滚轮与触摸操作不会穿透触发外层页底续滑换页。
- 案例的额外内容收进“补充材料”折叠栏，默认收起并可按需展开；只有确实配置了补充材料的案例才显示该栏，普通案例不会重复项目概述或保留空栏。
- 只有一张纵向主图的项目使用明确的响应式展示高度，并同时限制图片宽高、保持原比例完整居中，不用容器裁切换取铺满效果。
- 当前缩略图切换后自动滚动到视觉中心。
- 项目截图使用懒加载。
- 点击当前主图可打开整图灯箱。
- 灯箱支持关闭按钮、点击图片外的深色留白或说明区域、`Escape`、左右按钮和键盘方向键；点击图片本身不会误关。
- 手机端按图片比例自适应，左右控制区保持可操作。

### 原始作品链接

- 原始作品链接使用横向杂志索引结构，依次展示作品索引抬头、作品/平台数量，以及由左侧平台信息栏和右侧流动作品区组成的平台章节；521px 以上平台信息保持左侧，作品区按可用宽度自动横向分列，空间不足时减少列数而不压缩中文标题；520px 以下平台标题回到上方、作品使用单列，保证触控与阅读。
- 只有一个外部地址的项目不显示作品索引和平台计数，改用紧凑的在线产品入口：直接呈现产品名、域名与“打开在线体验”主按钮；当前“不牛马厨房”入口为 `https://freebite.zhechun.space`；桌面端横向排列，手机端上下排列。
- 展示作品名称，不直接堆叠长网址；每条作品行保留独立外链箭头、至少 44px 操作高度，以及悬停、按压和键盘焦点反馈。
- 新标签页链接使用 `noopener` 和 `noreferrer`；部分作品短链接按原始资料保留 HTTP。
- 原始作品链接位于案例主图与摘要之后的完整内容宽度内，使用统一的平台标签轨道与会随可用宽度自适应的作品内容场；小手机改为平台标题在上、作品单列，不采用等大卡片、瀑布流或跨平台混排。包含作品链接的案例窗口始终允许内部纵向滚动。
- 链接区延续深绿文字、浅绿纸面反馈和细金色操作提示，不再使用大圆角玻璃容器。

### AI 辅助视觉展示

- 品牌自媒体项目包含独立的 AI 辅助视觉区。
- 说明 AI 在视觉构思、场景生成和素材延展中的用途。
- 示例图可以进入同一灯箱查看。

### 响应式与可访问性

- 核心响应式区间覆盖桌面、平板和手机，重点关注 `960px`、`760px`、`680px` 和更窄屏幕。
- 交互元素具备键盘焦点和语义化辅助说明。
- `prefers-reduced-motion: reduce` 下关闭或压缩非必要动画，内容仍完整可用。
- `index.html` 包含中文语言、viewport、description 和页面标题。

## 7. 视觉系统与用户偏好

整体方向：低饱和自然系、生活方式杂志感、纸质颗粒、柔和毛玻璃和自然光影。

推荐关键词：

- 深苔墨绿 `#23483D`、雾蓝灰 `#7D9BA4`、暖米纸白 `#F5F1E8`
- 柔焦、胶片颗粒、轻量拼贴
- 真实摄影感、克制动效、自然节奏

避免：

- 科技风、赛博风、强渐变和复杂 3D
- 卡通感、模板化卡片和纯视觉炫技
- 首页人物主视觉
- 文字重叠、英文堆叠和图片堆砌
- 明显双层框、硬边框和无意义循环动画

按钮和关键操作优先使用中文。动画要表达状态、空间关系或操作反馈，不能只为“看起来会动”。

色彩规则：深苔墨绿是唯一主色，用于关键操作、标题和高优先级信息；雾蓝灰仅用于编号、分隔线、数据标记和交互辅助状态；暖米纸白承担大面积底色。避免再引入金色作为功能性强调色。

文字层级：独立内容页正文约 `15–16px`，条目说明约 `14px`，时间、标签和辅助信息约 `13px`，眉题与编号不低于 `12px`；手机端正文不低于约 `15px`，能力与流程说明不低于 `14px`。

字体与动效 Token：`--font-serif` 统一维护 EB Garamond 与中文宋体回退顺序；`--ease-editorial`、`--motion-fast`、`--motion-medium` 和 `--motion-content` 分别管理状态反馈、内容入场与页面切换节奏。页面入场使用 `opacity + translateY(14px)`，项目交互仅动画 `transform`、`opacity`、`filter` 与 `color`；`prefers-reduced-motion` 下关闭位移、缩放、分组淡化与 stagger。

## 8. 素材与代码位置

- `index.html`：网页标题、描述和入口节点。
- `src/main.jsx`：React 入口。
- `src/App.jsx`：导航、经历、项目、链接、轮播、灯箱、音频和主要组件逻辑。
- `src/ExperienceUmbrella.jsx`：关于页按需加载的 Three.js 参数化雨伞、立体履历卡、开合旋转、射线选择与 WebGL 生命周期逻辑。
- `src/index.css`：视觉系统、布局、断点、动效和低动态模式。
- `public/assets/`：站点当前使用的普通视觉素材；透明贴纸、物件图和 PNG 来源的作品截图统一使用高质量 WebP 发布版本，关键透明物件按实际显示尺寸保留约 2 倍采样并保留透明通道，动画 WebP 保留原有帧结构，未引用的历史版本移入本地忽略目录。
- `public/assets/mint-guitar-sticker.webp`：用户最新提供的薄荷绿电吉他透明贴纸，仅裁去透明画布留白，完整保留琴体与白色贴纸描边，用于首页右下侧可移动装饰。
- `public/assets/instax-mini8-butter-yellow-v4.webp`：由用户提供的奶油黄色 instax Mini 8 正面图抠取为透明背景；在 v02.58 收紧相机下边缘的半透明残留像素，用于关于页拍立得交互的相机前景。
- 历史联系页的绿色草地与流动光影素材已移入本地忽略目录，不参与当前发布。
- `public/assets/contact-window-cat-and-fish.webp`：用户提供的猫咪窗景方形图片，经 WebP 优化后用于联系页左上角。
- `public/assets/cat-sticker-v2.webp`：首页右下角使用的用户最新橘白猫透明贴纸，按实际透明边界紧裁并优化为 725×1200 发布素材。
- `public/assets/person-guitar-sticker-v2.webp`：首页左下角使用的人物吉他透明贴纸，已清理实底棋盘格并保留透明通道。
- `public/assets/guitar-sticker.webp`：人物贴纸案例中使用的吉他透明素材。
- `public/assets/green-grape-soda-sticker.webp`：首页左上留白区使用的用户提供青提气泡饮透明贴纸。
- `public/assets/turntable-player-sage-reference.webp`：首页动态唱片机使用的透明机身底图。
- `public/audio/just-keep-walking-michael-ramir-c.mp3`：唱片机默认曲目，从 Mixkit 官方下载；曲目来源、授权链接和文件校验值记录在同目录 `SOURCE.md`。
- `public/assets/projects-ccd-frame-silver.webp`：经历页使用的冷调银色 Sony CCD 透明成品，屏幕已排入最终标题与说明文字。
- `public/assets/projects-paper-plane-realistic.webp`：经历页 CCD 下方使用的真实白色折纸飞机透明位图，参考用户提供素材生成并保留折脊与翼面明暗。
- `public/assets/projects-film-canister-superia-200-powder-blue-clean.webp`：经历页项目总览使用的竖放富士 Superia 200 胶卷；在透明抠图基础上只将绿色标签确定性映射为低饱和雾霾粉蓝，并清除画布四周的白色半透明残留，保留原文字、紫色标签、圆柱端盖和出片侧材质。胶卷在桌面与窄屏均放大约两成，胶片画幅不缩小，上下齿孔数量按项目数以较疏密度生成。
- `public/assets/strengths-clipboard-panel.webp`：优势页章节开场使用的透明浅蓝夹板成品，保留金属夹、毛边白纸和自然投影，网页文字独立叠在纸张安全区内。
- `public/assets/skill-wheel-girl-*.webp`：Hatch Wheel Pet 案例使用的粉色小厨娘四状态透明动画，分别对应待机、左右奔跑和完成反馈。
- `public/assets/skill-interview-evidence-map.webp`：Prepare and Review Interviews 案例的匿名化证据链结构图，不包含姓名、具体公司或私人资料。
- `public/assets/entry-walking-cat.webp`、`entry-walking-cat-animated.webp`：由用户指定的小猫参考视频提取的同造型透明静态帧与 1.12 秒循环步态；静态帧用于窗内焦点与低动态模式，完整步态用于加载轨道。
- `src/assets/fonts/liyu-shoushu-postcard.woff2`：由漓雨手书 0.107 原版 TTF 按明信片实际字符生成的本地字体子集，仅用于关于页明信片背面正文与收寄信息；来源和 SIL OFL 1.1 授权说明保存在同目录。
- `src/assets/fonts/real-typewriter-strengths.woff2`：由迫真打字油印体原版 TTF 按优势页夹板实际字符生成的本地字体子集，仅用于夹板内眉题、标题和正文；来源与免费商用授权摘要保存在同目录。
- `public/media/ai-design/`：AI 辅助视觉示例。
- `public/media/pdf-images/`：当前项目实际引用的作品截图。
- `.github/workflows/deploy-pages.yml`：Pages 自动部署。
- `AGENTS.md`：AI 协作、视觉和验收规范。
- `skills/ui-ux-pro-max/SKILL.md`：项目本地 UI/UX 设计与审查 skill，覆盖无障碍、触控、响应式、字体配色和动效优先级。
- `项目概述.md`：当前产品与技术事实。
- `项目进度表.md`：版本化修改记录。

未使用素材保存在本地 `.local-unused-assets/`，旧文档原稿保存在 `.local-docs/`；两者均被 Git 忽略。

## 9. 维护入口

- 修改经历、项目、指标、能力和链接：编辑 `src/App.jsx` 对应数据与组件。
- 修改视觉、布局、断点和动效：编辑 `src/index.css`。
- 修改标题和 SEO 描述：编辑 `index.html`。
- 新增项目截图：放入 `public/media/pdf-images/`，并在 `projectImages` 中显式绑定。
- 新增普通视觉素材：放入 `public/assets/`，并通过 `assetUrl`、模块导入或 Vite 可处理的 CSS 路径引用。
- 不要手动修改 `dist/`；每次交付前重新执行构建。
- 修改功能或结构后，同步更新本文档；任何修改都要同步 `项目进度表.md`。

## 10. 验收要求

每次页面改动至少检查：

- 生产构建是否成功。
- 桌面端和移动端是否横向溢出。
- 首次加载、滚动、菜单、轮播、灯箱、外链和邮件入口。
- 控制台是否出现错误或资源 404。
- 动效是否自然，并兼容 `prefers-reduced-motion`。
- 新增素材是否真正被引用，是否包含不应公开的信息。

## 11. 后续事项

- 首次发布前人工检查作品截图中的账号、二维码、用户信息和业务数据。
- 定期复核外部短链接是否仍有效，避免失效跳转或目标变化。
- 可补充 favicon、社交分享图和更完整的分享元信息。
- 可继续细化项目目标、成果标签和 AI 使用案例。
- 可进一步强化联系页的整屏收尾效果，但应保持当前视觉体系。
