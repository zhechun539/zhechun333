# 折椿个人作品集项目概述

当前版本：`v01.78`

本文档由原《个人作品集网站制作进度与交接说明》和《网站制作进度与功能说明》整理迁移而来，用于帮助新协作者快速理解项目。内容以当前代码为准，已剔除过期实现、旧仓库信息、本机路径和私人联系方式。

## 1. 项目定位

这是一个面向求职展示的菜单式新媒体运营作品集。公开名称为“折椿”，目标方向包括内容策划、账号运营、品牌内容编辑和产品运营。

网站重点传达：

- 内容策划与选题判断
- 小红书、公众号、短视频等平台运营经验
- 项目协同、互动承接、数据复盘和转化意识
- 审美判断、内容组织与品牌表达能力
- 将 AI 用于内容、视觉和产品实践的能力

文案不主动强调毕业年份或经验年限，优先展示可验证的项目过程和成果。

## 2. 当前状态

- 菜单式作品集主体已经完成，五个一级模块按独立视图切换展示。
- 页面包含首屏、经历、精选项目、优势和联系模块。
- 当前公开名称为“折椿”。
- 页面保留求职邮件入口，不展示手机号和微信号。
- 项目可以执行生产构建，并通过 GitHub Actions 发布到 GitHub Pages。
- 线上地址为 `https://resume.zhechun.space/`；GitHub 项目地址 `https://zhechun539.github.io/zhechun333/` 同样可访问。
- 首次正式发布前仍需人工复核作品截图内的账号、二维码和业务数据。

## 3. 技术栈

- React 19
- Vite 8
- Lucide React
- 原生 CSS
- Web Audio API
- GitHub Actions
- GitHub Pages

全站使用 EB Garamond 与中文宋体回退栈混排：英文、数字和罗马编号通过 Google Fonts 官方样式加载 EB Garamond 400 / 500，中文优先使用本机 Noto Serif SC、思源宋体、Songti SC、STSong 或 SimSun；首页中文品牌标题“折椿”单独使用 `073 TEGUSE — Kanaka Font（鹿仲茉菜の手書）`，通过 ZeoSeven FontsAPI 按字符子集加载，失败时回退至本机楷体。项目不保存或分发该字体源文件；字体作者将 Web Font 嵌入标记为需咨询，公开发布前必须确认许可。项目是纯前端静态网站，不包含后端、数据库、登录、支付或用户数据收集功能。

## 4. 运行、构建与发布

安装依赖：

```powershell
npm.cmd install
```

本地开发：

```powershell
npm.cmd run dev -- --port 5174 --strictPort
```

也可直接双击根目录的 `启动本地预览.bat`：首次运行会安装依赖，随后启动本地开发服务器并自动打开预览页；保持命令窗口打开，按 `Ctrl+C` 停止服务。

本地预览路径：

```text
http://127.0.0.1:5174/zhechun333/#home
```

生产构建：

```powershell
npm.cmd run build
```

构建产物位于 `dist/`，不手动维护、不提交 Git。`vite.config.js` 使用 `base: './'`，使资源 URL 相对于当前 HTML 页面生成，同时兼容 GitHub Pages 项目子路径和自定义域名根路径。React 中的公开素材通过 `import.meta.env.BASE_URL` 生成地址，CSS 中的素材使用可由 Vite 构建转换的相对路径。

`.github/workflows/deploy-pages.yml` 在 `main` 分支更新后执行：

1. 检出源码。
2. 安装 Node.js 和 npm 依赖。
3. 执行生产构建。
4. 上传 `dist/`。
5. 发布 GitHub Pages。

## 5. 页面结构

### 5.1 首屏 `#home`

- 使用低饱和自然系杂志封面构图。
- 主视觉为森林与白鸟，不使用人物照片。
- 主图：`public/assets/hero-forest-photo.webp`。
- 辅助视觉：`hero-stream-flowers.webp`、`hero-lily-water.webp`、`hero-wisteria-canopy.webp`。
- 标题使用公开名称“折椿”，以 073 TEGUSE 的不规则日常手写字作为首页专属签名字体；副标题 `Editorial Content Portfolio` 继续使用 EB Garamond，形成手写中文与编辑式英文的对比。
- 主要入口为“精选项目”和“联系我”。
- 包含颗粒、自然光影、稀疏雨滴、柔和显现和克制的图像运动。
- `RainMusicButton` 使用 Web Audio API 实时合成雨幕、环境和弦与稀疏旋律，不加载第三方音乐文件；受浏览器自动播放限制，必须由用户点击开启。

### 5.2 关于与履历 `#about`

- 个人照片：`public/media/pdf-images/full_p02_i01_Im73.jpg`。
- 照片与求职方向组成横向编辑式人物档案：桌面端照片靠左、求职方向与能力说明在右，中间以留白和细线建立关系，不再将大幅人像单独居中；移动端改为左对齐横幅照片与略向右错位的信息区，保持自然阅读节奏。
- “运营 / 策划 / 产品”标签可切换能力说明。
- 邮箱入口整合进求职方向信息面板的标题行，与“求职方向”标签并列；不再在履历上方单独占用一行。
- 桌面端有轻微指针景深跟随和柔光反馈。
- 关于页在所有尺寸都采用“页面简介 → 横向人物档案 → 职业履历”的纵向叙事顺序；人物档案内部桌面为左图右文，履历仍在其下方独立展开。
- 关于页主标题固定为“偏内容创意型的 / 新媒体运营。”两行，避免“新”字孤立在第一行末尾。
- 履历采用单列职业索引，区分正式工作、实习、线上兼职和阶段性项目；每段按“编号与时间 → 岗位与公司 → 职责 / 成果”纵向组织，保留最能吸引招聘者的工作动作或可验证结果。各行按顺序轻量入场，并在低动态设置下停用动画。

### 5.3 经历总览与案例窗口 `#projects`

当前以内容运营岗位相关性排序，包含：

1. 品牌自媒体运营
2. 小红书种草协同
3. 教育类小红书获客
4. B 端软件内容运营
5. 旅游攻略内容引流
6. 热点传播与官方账号
7. 个人账号运营
8. 内容写作与补充作品
9. 不牛马厨房 AI 产品实践（补充能力）

章节使用“超大低透明度 PROJECTS 水印 + 英文眉题 + 中文标题与说明”的两层标题。九个项目继续按岗位相关性排序，但总览改为两级编辑结构：前 3 个重点运营案例使用三列 3:4 纵向精选图版，显示罗马编号、项目名、类型和首项成果；剩余 6 个项目进入带罗马编号、名称、摘要、类型与阅读入口的横向项目编目。桌面端精选图版在悬停或键盘聚焦时降低非当前项透明度，当前图版只做极轻的对比度、饱和度与位移反馈；760px 以下两组都回到完整单列，不依赖悬停显示信息。点击任意图版或编目行后，继续打开原有案例阅读窗口；窗口与内部轮播、灯箱、原始作品链接、键盘和浏览器返回交互保持不变。项目图片在 `src/App.jsx` 的 `projectImages` 中配置，文字与链接在项目数据中维护。

### 5.4 优势 `#strengths`

- 六步流程：内容判断、选题策划、内容制作、发布运营、互动承接、数据复盘。
- 能力方向：内容策略、互动转化、多平台节奏、项目协同、基础视觉与剪辑。
- 优势页采用“工作方法 / 核心能力”双栏编辑结构：桌面端左侧为六步纵向运营链路，右侧为六项纵向能力清单；窄屏按工作方法、核心能力顺序叠放。编号、连接线与图标统一为圆形索引语言，条目之间以细分隔线组织，不使用密集横向流程或三列小字矩阵。
- 模块保留 AI 使用相关表达，可继续补充具体工具、工作流或案例。

### 5.5 联系 `#contact`

- 作为整页收尾，保留求职表达和邮件联系入口。
- 联系页使用 `public/assets/contact-green-light.png` 作为全屏背景，并叠加透明森林绿遮罩保证文字可读；不使用网格底纹、矩形色块、背景框或卡片容器。
- 排版保留章节页码与三段大标题，信息区只呈现“求职联系”邮箱，不再出现“了解完整作品”或需要额外索取作品材料的说明。邮箱为透明无框文字入口，保留至少 58px 触控高度。
- 联系页标题使用更舒展的行距；移动端三段标题各自保持完整单行，不拆分末尾文字。
- 不展示手机号和微信号。

补充：第 09 项的 AI 表述仅用于说明辅助制作流程，用户侧功能为餐食决策、随机转盘和偏好筛选；桌面端海报与叙事采用均衡双栏。普通案例的原始作品链接作为主图与项目摘要之后的独立全宽索引区呈现，不挤占右侧摘要，也不占用底部折叠区域。

## 6. 已实现功能

### 导航与页面反馈

- 桌面端固定完整导航，移动端使用菜单按钮；点击“首页 / 关于 / 经历 / 优势 / 联系”后只展示对应的独立内容视图，不再把五个模块连续铺成长页。
- 当前视图由 URL Hash 保存，可直接访问、刷新保留，并支持浏览器前进与后退；导航使用 `aria-current="page"` 标识当前页。
- 顶部菜单与浏览器前进后退切换视图时内容轻量显现；五个视图（包括首页）共用页底续滑轨道，续滑换页时目标章节同步挂载并在浏览器绘制前直接位于 `scrollY: 0`，随后以约 440ms 的轻微上移与缩放弹入建立前后章节的空间连续性；只阻止实际触发换页的那一次滚轮或触摸事件，后续连续输入会立即用于正常浏览新章节。低动态模式关闭这段位移与缩放。
- 独立页统一使用超大低透明度英文水印、英文眉题、中文正式标题、细分隔线和纵向留白组织章节；关于、经历、优势和联系保持各自原有内容顺序。
- 当前导航除文字状态外，在菜单项下方显示约 5px 小圆点，并继续保留 `aria-current="page"`。
- 五个独立视图均提供居中的连续浏览入口：首页→关于→经历→优势→联系，联系页提供返回首页。页底只预留约 96–124px 的纯背景轻拉轨道，并为短页面同步增加包含轨道的最小总高度；首次下拉达到约 34px 时，无需停止输入便会立即触发自然归位并显示下一章节提示，归位期间的继续下滑不会与动画互相拉扯。提示出现并完成短暂解锁后，再次向下滚动累计约 44px 或触摸上滑约 25px 即可切换章节。归位由当前输入速度驱动：输入越快，触发后继续向下的惯性距离越长，但最多只额外延伸约 30px；到达惯性最低点后先稳定实际滚动位置，再以浏览器合成层的亚像素位移在约 460–720ms 内将页面内容柔和归位，避免短距离逐帧滚动的阶梯感。无边框文字与箭头从底部小幅上移并缓慢渐显；完成入场后两者共用 2.8 秒呼吸周期，箭头向下约 6px时文字同步轻微缩小到 0.99 并降低至约 0.8 透明度，箭头回到上方时文字恢复完整清晰。向上滚动会立即取消待执行归位。普通页面提示使用低饱和浅苔绿，联系页使用浅鼠尾草绿；低动态模式关闭呼吸并即时归位。
- 导航底部显示当前视图的页内阅读进度。
- 离开首屏后显示返回顶部按钮。
- 锚点使用平滑滚动。
- 经历、项目、优势和联系内容首次进入视口时自然显现。

### 项目轮播与灯箱

- 当前选中的项目包含当前主图、前后预览、左右切换和缩略图条；未选项目只加载紧凑索引。
- 案例阅读窗口打开期间，滚轮与触摸滚动只作用于窗口内部，不会穿透触发外层页底续滑换页。
- 只有一张纵向主图的项目使用明确的响应式展示高度，并同时限制图片宽高、保持原比例完整居中，不用容器裁切换取铺满效果。
- 当前缩略图切换后自动滚动到视觉中心。
- 项目截图使用懒加载。
- 点击当前主图可打开整图灯箱。
- 灯箱支持关闭按钮、点击图片外的深色留白或说明区域、`Escape`、左右按钮和键盘方向键；点击图片本身不会误关。
- 手机端按图片比例自适应，左右控制区保持可操作。

### 原始作品链接

- 原始作品链接使用横向杂志索引结构，依次展示作品索引抬头、作品/平台数量，以及由左侧平台信息栏和右侧流动作品区组成的平台章节；521px 以上平台信息保持左侧，作品区按可用宽度自动横向分列，空间不足时减少列数而不压缩中文标题；520px 以下平台标题回到上方、作品使用单列，保证触控与阅读。
- 只有一个外部地址的项目不显示作品索引和平台计数，改用紧凑的在线产品入口：直接呈现产品名、域名与“打开在线体验”主按钮；当前“不牛马厨房”入口为 `https://freebite.zhechun.space`；桌面端横向排列，手机端上下排列。
- 展示作品名称，不直接堆叠长网址；每条作品行保留独立外链箭头、至少 44px 操作高度，以及悬停、按压和键盘焦点反馈。
- 新标签页链接使用 `noopener` 和 `noreferrer`；部分作品短链接按原始资料保留 HTTP。
- 原始作品链接位于案例主图与摘要之后的完整内容宽度内，使用统一的平台标签轨道与会随可用宽度自适应的作品内容场；小手机改为平台标题在上、作品单列，不采用等大卡片、瀑布流或跨平台混排。包含作品链接的案例窗口始终允许内部纵向滚动。
- 链接区延续深绿文字、浅绿纸面反馈和细金色操作提示，不再使用大圆角玻璃容器。

### AI 辅助视觉展示

- 品牌自媒体项目包含独立的 AI 辅助视觉区。
- 说明 AI 在视觉构思、场景生成和素材延展中的用途。
- 示例图可以进入同一灯箱查看。

### 响应式与可访问性

- 核心响应式区间覆盖桌面、平板和手机，重点关注 `960px`、`760px`、`680px` 和更窄屏幕。
- 交互元素具备键盘焦点和语义化辅助说明。
- `prefers-reduced-motion: reduce` 下关闭或压缩非必要动画，内容仍完整可用。
- `index.html` 包含中文语言、viewport、description 和页面标题。

## 7. 视觉系统与用户偏好

整体方向：低饱和自然系、生活方式杂志感、纸质颗粒、柔和毛玻璃和自然光影。

推荐关键词：

- 深苔墨绿 `#23483D`、雾蓝灰 `#7D9BA4`、暖米纸白 `#F5F1E8`
- 柔焦、胶片颗粒、轻量拼贴
- 真实摄影感、克制动效、自然节奏

避免：

- 科技风、赛博风、强渐变和复杂 3D
- 卡通感、模板化卡片和纯视觉炫技
- 首页人物主视觉
- 文字重叠、英文堆叠和图片堆砌
- 明显双层框、硬边框和无意义循环动画

按钮和关键操作优先使用中文。动画要表达状态、空间关系或操作反馈，不能只为“看起来会动”。

色彩规则：深苔墨绿是唯一主色，用于关键操作、标题和高优先级信息；雾蓝灰仅用于编号、分隔线、数据标记和交互辅助状态；暖米纸白承担大面积底色。避免再引入金色作为功能性强调色。

文字层级：独立内容页正文约 `15–16px`，条目说明约 `14px`，时间、标签和辅助信息约 `13px`，眉题与编号不低于 `12px`；手机端正文不低于约 `15px`，能力与流程说明不低于 `14px`。

字体与动效 Token：`--font-serif` 统一维护 EB Garamond 与中文宋体回退顺序；`--ease-editorial`、`--motion-fast`、`--motion-medium` 和 `--motion-content` 分别管理状态反馈、内容入场与页面切换节奏。页面入场使用 `opacity + translateY(14px)`，项目交互仅动画 `transform`、`opacity`、`filter` 与 `color`；`prefers-reduced-motion` 下关闭位移、缩放、分组淡化与 stagger。

## 8. 素材与代码位置

- `index.html`：网页标题、描述和入口节点。
- `src/main.jsx`：React 入口。
- `src/App.jsx`：导航、经历、项目、链接、轮播、灯箱、音频和主要组件逻辑。
- `src/index.css`：视觉系统、布局、断点、动效和低动态模式。
- `public/assets/`：当前首屏使用的自然视觉素材；照片类首屏图使用高质量 WebP，避免部署 PNG 原图造成首次加载延迟。
- `public/assets/contact-green-light.png`：联系页使用的用户提供绿色草地与流动光影背景；后续可无损观感地转换为高质量 WebP 以减少体积。
- `public/media/ai-design/`：AI 辅助视觉示例。
- `public/media/pdf-images/`：当前项目实际引用的作品截图。
- `.github/workflows/deploy-pages.yml`：Pages 自动部署。
- `AGENTS.md`：AI 协作、视觉和验收规范。
- `项目概述.md`：当前产品与技术事实。
- `项目进度表.md`：版本化修改记录。

未使用素材保存在本地 `.local-unused-assets/`，旧文档原稿保存在 `.local-docs/`；两者均被 Git 忽略。

## 9. 维护入口

- 修改经历、项目、指标、能力和链接：编辑 `src/App.jsx` 对应数据与组件。
- 修改视觉、布局、断点和动效：编辑 `src/index.css`。
- 修改标题和 SEO 描述：编辑 `index.html`。
- 新增项目截图：放入 `public/media/pdf-images/`，并在 `projectImages` 中显式绑定。
- 新增普通视觉素材：放入 `public/assets/`，并通过 `assetUrl`、模块导入或 Vite 可处理的 CSS 路径引用。
- 不要手动修改 `dist/`；每次交付前重新执行构建。
- 修改功能或结构后，同步更新本文档；任何修改都要同步 `项目进度表.md`。

## 10. 验收要求

每次页面改动至少检查：

- 生产构建是否成功。
- 桌面端和移动端是否横向溢出。
- 首次加载、滚动、菜单、轮播、灯箱、外链和邮件入口。
- 控制台是否出现错误或资源 404。
- 动效是否自然，并兼容 `prefers-reduced-motion`。
- 新增素材是否真正被引用，是否包含不应公开的信息。

## 11. 后续事项

- 首次发布前人工检查作品截图中的账号、二维码、用户信息和业务数据。
- 定期复核外部短链接是否仍有效，避免失效跳转或目标变化。
- 可补充 favicon、社交分享图和更完整的分享元信息。
- 可继续细化项目目标、成果标签和 AI 使用案例。
- 可进一步强化联系页的整屏收尾效果，但应保持当前视觉体系。
