# 项目进度表

当前版本：`v01.78`

本文档继承旧项目说明中的可追溯记录。新记录必须添加在“更新记录”下方，保持最新版本在最前。版本号使用 `vXX.XX` 格式。

## 更新记录

### v01.78 — 2026-07-30

- 修改范围：页底首次下拉达到提示阈值后的自动显现与归位时机。
- 具体变化：首次下拉越过约 34px 阈值时，不再等待滚轮或触摸输入停止才执行归位；滚动状态检测会立即启动既有惯性归位并同步显示“继续向下滑动”提示。归位进行期间继续向下的滚轮或触摸输入会被短暂接管，避免页面与归位动画互相拉扯；向上输入仍可取消归位。v01.77 的 44px 滚轮与 25px 触摸二次换页阈值保持不变。
- 验证结果：`npm.cmd run build` 与 `git diff --check` 通过。浏览器桌面端首次连续下滑越过阈值后，在原 240–300ms 松手等待计时结束前的 100ms 检查点，提示已为 `aria-hidden=false` 且保持在 `#home`；归位期间继续向下输入未触发误切页。390×844 移动端目标视口同样在 100ms 检查点自动显示提示，继续下滑时页面保持归位位置，归位完成后轻滑 45px 正常进入 `#about`并从 `scrollY: 0` 开始。桌面与移动端均无横向溢出，控制台无错误或警告；低动态覆盖规则保持有效。
- 遗留事项：沿用 v01.65 的背景图体积优化事项与 v01.59 的字体授权待确认事项。

### v01.77 — 2026-07-30

- 修改范围：页底下一章节提示出现后的二次下滑换页灵敏度。
- 具体变化：保留“首次下拉显示提示、再次下滑切页”的两段式交互，将提示出现后的滚轮累计换页阈值从 132px 降为 44px，并将触摸上滑阈值从 76px 降为 25px，均约为原值三分之一；用户看到“继续向下滑动”后，只需一次轻量下滑即可进入下一章节。
- 验证结果：`npm.cmd run build` 与 `git diff --check` 通过。浏览器桌面端实测首页提示稳定出现后，先下滑 30px仍停留 `#home`，再追加 15px、累计达到 45px后立即进入 `#about`并从 `scrollY: 0` 开始；390×844 移动端目标视口在提示稳定后一次下滑 45px同样进入 `#about`。桌面与移动端均无横向溢出，控制台无错误或警告；浏览器加载后的样式表仍包含章节入场对应的 `prefers-reduced-motion` 覆盖规则。
- 遗留事项：沿用 v01.65 的背景图体积优化事项与 v01.59 的字体授权待确认事项。

### v01.76 — 2026-07-30

- 修改范围：项目案例阅读窗口与外层续滑换页的滚动事件隔离。
- 具体变化：页底续滑监听现在会识别项目弹窗的打开状态及来自遮罩内部的滚轮和触摸事件，并在弹窗接管输入时清空外层累计距离、速度与待执行归位，同时取消已经启动的外层惯性帧和合成层归位动画；弹窗内部仍保持原有纵向滚动，但不会再推动外层页面、累积换页阈值或误切到下一章节。
- 验证结果：`npm.cmd run build` 与 `git diff --check` 通过。浏览器在 1440×900 与 390×844 目标视口打开“品牌自媒体运营”案例并将内部滚动至底部后，继续多次向下滚动，弹窗均保持打开、Hash 均保持 `#projects`，内部滚动停在各自最大值，外层 `window.scrollY` 在边界输入前后完全一致；两种尺寸均无横向溢出，控制台无错误或警告。浏览器加载后的样式表仍包含章节入场对应的 `prefers-reduced-motion` 覆盖规则。
- 遗留事项：沿用 v01.65 的背景图体积优化事项与 v01.59 的字体授权待确认事项。

### v01.75 — 2026-07-30

- 修改范围：首页页底续滑轨道与五个章节的续滑切页入场反馈。
- 具体变化：修复首页虽渲染连续浏览入口、却未像后续章节一样预留 `--chapter-runway` 高度的问题；首页现在保持原首屏内容可用高度不变，并在其后增加同规格的 96–124px 轻拉轨道，使首页第一次下滑也能显示“下一部分 · 关于”，再次下滑进入关于页。页底续滑导航由无入场反馈改为目标章节约 440ms 的轻微向上弹入，以 18px 位移、0.992 到 1 的克制缩放和透明度恢复表达进入下一页；菜单与浏览器导航仍沿用原显现逻辑。低动态模式完全关闭新增位移、缩放和渐显。
- 验证结果：`npm.cmd run build` 与 `git diff --check` 通过。浏览器桌面端实测首页总高度比视口多出 124px 轨道，首次下滑后提示完整显现并稳定在约 52px 的停留位置，再次下滑正确进入 `#about`；关于页拉到底后同样显示“下一部分 · 经历”，再次下滑进入 `#projects`。两次切页均从 `scrollY: 0` 开始，目标容器使用 `portfolioChapterEnter`、时长 0.44 秒；移动端复测首页→关于同样通过。桌面和移动端均无横向溢出，控制台无错误或警告；浏览器加载后的低动态媒体规则确认将新增切页的 `animation-name` 与 `transform` 置为 `none`。
- 遗留事项：沿用 v01.65 的背景图体积优化事项与 v01.59 的字体授权待确认事项。

### v01.74 — 2026-07-30

- 修改范围：基于输入速度的页底惯性延伸、合成层自然归位与同步呼吸提示。
- 具体变化：新增滚轮与触摸下滑速度采样，以平滑后的像素/毫秒速度决定松手后的惯性距离与时长。第一阶段按速度继续向下延伸约 5–30px并逐渐减速；第二阶段先把实际滚动位置稳定在提示停留点，再由浏览器合成层以亚像素位移动画将页面内容从惯性最低点柔和归位，回程时长按距离与速度限制在约 460–720ms，消除十几像素长时间逐帧滚动造成的阶梯卡顿。松手识别等待按速度在约 240–300ms 间调整。文字与箭头完成首次底部渐显后，共用 2.8 秒周期、同一缓动与延迟：箭头在上方时文字最大且最清晰，箭头下沉约 6px 时文字同步变为约 0.8 透明度与 0.99 缩放，形成关联呼吸而非闪烁。向上输入会清空速度并取消惯性时间线；低动态模式关闭惯性位移及呼吸动画。保留 v01.73 的轻拉轨道与浅绿色无框提示。
- 验证结果：`npm.cmd run build` 通过。浏览器实测文字与箭头的动画时长均为 2.8 秒、延迟均为 0.9 秒；箭头接近最低点时，文字同步处于约 0.81 透明度和 0.99 缩放，提示关系正确。页面运行无控制台错误，低动态媒体规则继续禁用提示动画；差异检查通过。
- 遗留事项：沿用 v01.65 的背景图体积优化事项与 v01.59 的字体授权待确认事项。

### v01.73 — 2026-07-30

- 修改范围：页底拉伸距离、归位运动语言和下一章节提示的慢速显现。
- 具体变化：放弃与自然杂志风不匹配的弹簧振荡方案，将轨道由 220–280px 收回至约 96–124px，首次提示阈值降至 34px、稳定留白调整为 52px。归位曲线改为约 900ms 的正弦缓入缓出，只做一次连续平滑位移，不再过冲、往返或快速改变方向；最后一次向下输入后的等待调整为 300ms，避免触控板事件间隙造成提前归位。提示整体入场距离缩短至 18px，文字和箭头使用 680–860ms 的慢速透明度与位移、120–190ms 的轻微错峰从底部渐显；删除箭头循环上下动画。修复普通页面提示颜色仍被后置旧规则覆盖的问题，最终使用低饱和浅苔绿，联系页继续使用浅鼠尾草绿。
- 验证结果：`npm.cmd run build` 与 `git diff --check` 通过。浏览器实测关于页弹性轨道约 100px，拉出约 83px后在等待期仍保持提示隐藏，随后以单向平滑位移归位并稳定在约 52px；最终提示为浅苔绿 `rgb(79, 114, 95)`，辅助文字为 `rgb(110, 140, 123)`，箭头 `animation-name: none`。联系页短页面的实际可滚动距离与轨道均约 100px，提示为浅鼠尾草绿 `rgb(222, 228, 211)`，背景透明、边框为 0。459×993 窄屏实测正文提示字号约 15px且无横向溢出；低动态媒体规则保留，控制台无错误或警告。
- 遗留事项：沿用 v01.65 的背景图体积优化事项与 v01.59 的字体授权待确认事项。

### v01.72 — 2026-07-30

- 修改范围：弹性页底的下拉纵深、回落阻尼、提示入场节奏与颜色重量。
- 具体变化：将纯背景弹性轨道由约 156–190px 延长至 220–280px，并把独立章节的最小总高度同步扩展为 `100dvh + 弹性轨道`，避免首页、联系页等短页面仍被原有最小高度压缩成约 84px 的实际可滚动距离。提示稳定位置由 76px 调整为 88px；回弹等待由 200ms 放宽至 260ms，逐帧回落由 460ms 的高频振荡改为约 720ms 的低频高阻尼曲线，减少急停和机械弹跳。提示本体取消缩放和过冲曲线，改为 24px 的短距离缓入；文字与箭头分别使用透明度、7–8px 位移和 70–120ms 轻微错峰进入。普通页面提示改为低饱和浅苔绿，辅助文字与箭头再降低对比度；联系页改为偏灰浅鼠尾草绿，继续保持透明无框。
- 验证结果：待完成生产构建、差异检查及首页、长页面、联系页、移动端与低动态验收。
- 遗留事项：沿用 v01.65 的背景图体积优化事项与 v01.59 的字体授权待确认事项。

### v01.71 — 2026-07-29

- 修改范围：弹性页底的方向判断、触发高度与提示联动时机。
- 具体变化：将首次提示阈值由 84px 降至 52px、提示出现后的回弹停留距离由 96px 降至 76px，改善首页可滚动距离较短时难以触发的问题；回弹等待调整为最后一次向下输入后的 200ms，给触控板惯性留下连续拉伸空间。新增滚动方向状态：向上滚动会取消尚未执行的回弹及正在进行的回弹帧，不再把用户拉回页底。首次下拉越过阈值时只记录状态，不立即显示提示；松手开始回弹时才同步显现文字与箭头，使提示入场与页面回弹成为同一个动作。保留无框提示、第二阶段 132px 换页门槛及换页后立即恢复普通滚动。
- 验证结果：`npm.cmd run build` 与 `git diff --check` 通过。浏览器确认首页弹性区总可滚动距离约 83px，新的 52px 阈值可在短页面内正常达到，越阈值后回弹停在约 76px；关于页显示提示后向上滑动约 45px，等待 700ms 后仍保持新的上滑位置，未被重新拉回，继续向上离开弹性区后提示由可见平滑收起至 `opacity: 0`。首次提示改为在回弹函数内使用同步状态提交，与回弹在同一帧启动；复测提示稳定位置约 76px、无横向溢出，第二阶段未达门槛不换页，达到门槛后进入经历页，后续滚动停在 240px且不归零。窄屏交互沿用同一手势状态机，`prefers-reduced-motion` 规则存在，控制台无错误或警告。
- 遗留事项：沿用 v01.65 的背景图体积优化事项与 v01.59 的字体授权待确认事项。

### v01.70 — 2026-07-29

- 修改范围：页底续滑提示的空间形态、入场动效与换页阈值。
- 具体变化：将续滑入口改为“弹性页底轨道 + 无框浮动提示”。每页正文结束后保留约 156–190px 的纯背景缓冲，用户到达内容底部后仍能随惯性继续下拉；下拉不足约 84px并松手时回弹到原页底，越过阈值后才显示文字与箭头，并弹回留有安全空白的位置，避免覆盖末尾正文。删除浮层的边框、底色、阴影和模糊背景；提示稳定后，继续滚动累计 132px 或触摸上滑 76px 才切换章节。回弹使用带轻微阻尼的原生逐帧位移，低动态模式改为即时归位；换页后的新页面不设置滚动锁。
- 验证结果：`npm.cmd run build` 与 `git diff --check` 通过。桌面端实测关于页自然内容底部为 1698px、弹性轨道为 158px：继续下拉约 70px后松手会回到内容底部且提示保持隐藏；下拉超过阈值后提示显示，并回弹停在约 96px 的安全留白处。提示计算样式为透明背景、0px 边框、无阴影，固定定位后的底边位于 830px 视口内约 812px。提示稳定后，100px 的下滑不会换页，150px 的明确下滑进入经历页；新页继续滚动到 240px并保持稳定，未被拉回顶部。窄屏实际视口复测同样完成“越阈值显现 → 次次下滑换页 → 新页继续滚动”，无横向溢出；样式表保留 `prefers-reduced-motion: reduce` 覆盖，控制台无错误或警告。
- 遗留事项：沿用 v01.65 的背景图体积优化事项与 v01.59 的字体授权待确认事项。

### v01.69 — 2026-07-29

- 修改范围：连续滚动跨章节后的新页面浏览。
- 具体变化：删除换页后持续约 500ms 的全局滚轮 / 触摸拦截、反复 `scrollTo(0)` 以及对应的 `is-chapter-jump-locked` 样式。现在只阻止达到阈值并实际触发换页的那一次事件；旧页预先回顶、目标章节 `flushSync` 同步挂载并再次校正顶部的首帧逻辑继续保留，后续滚轮或触摸输入会立即用于正常滚动新章节，不再把持续滑动误判为需要清除的残余惯性。
- 验证结果：`npm.cmd run build` 与 `git diff --check` 通过；桌面端实测“关于 → 经历”，触发换页时 `scrollY` 为 0，不停手继续滚动后立即到 320px，等待 650ms 后仍为 320px，未再归零。390×844 移动端同路径复测，换页后连续滚动到约 260px并保持稳定，无横向溢出；页面样式表包含 `prefers-reduced-motion: reduce` 覆盖，控制台无错误或警告。
- 遗留事项：沿用 v01.65 的背景图体积优化事项与 v01.59 的字体授权待确认事项。

### v01.68 — 2026-07-29

- 修改范围：页底提示显现后的纯滚轮换页可用性。
- 具体变化：修复 v01.67 将滚轮静止等待设为 650ms，且在等待期间每个连续滚轮事件都会重新计时，导致触控板用户持续向下滑动时无法触发换页、只能点击入口的问题。提示出现后的解锁时间缩短为约 220ms，等待期间的连续滚轮事件不再重置计时；解锁后继续向下滚动累计达到原阈值即可换页。保留同步挂载、首帧置顶、残余惯性隔离、点击、键盘和触控支持。
- 验证结果：`npm.cmd run build` 与 `git diff --check` 通过；桌面端全程仅使用滚轮完成“关于 → 经历”，提示显现后再次下滑即进入 `#projects`，即时与稳定滚动位置均为 0，未点击任何入口；390×844 目标视口仅使用滚动完成“联系 → 首页”，即时与稳定位置同样为 0且无横向溢出。控制台无错误或警告。
- 遗留事项：沿用 v01.65 的背景图体积优化事项与 v01.59 的字体授权待确认事项。

### v01.67 — 2026-07-29

- 修改范围：页底续滑换页与滚轮惯性的彻底分离。
- 具体变化：删除续滑路径上的整页退出、背景承接和新页进入动画，以及对应 520ms / 700ms 延迟；第二次向下滚动现在只负责发出换页指令。触发后使用 History API 更新 Hash、`flushSync` 同步挂载目标章节，并在同一浏览器事件内先后将旧页和新页滚动位置设为 0，使新章节第一帧天然位于顶部。为避免同一次触控板惯性继续滚动新页，触发事件改为非被动监听并调用 `preventDefault`，随后临时关闭滚动锚定并拦截残余滚轮 / 触摸事件，直到最后一个事件停止 500ms 后解除；页底提示须在上一段滚轮静止 650ms 后才允许下一次触发，保证“到达底部显示提示”和“再次滚动换页”是两个独立手势。
- 验证结果：`npm.cmd run build` 与 `git diff --check` 通过；桌面端复测“关于 → 经历”，首次到达页底只显示提示，再次滚动后第一次状态读取即为 `#projects`、`scrollY: 0`、`animation-name: none`，惯性锁解除后仍为 0；390×844 目标视口复测“联系 → 首页”，即时与稳定状态均为 `scrollY: 0`，无横向溢出。控制台无错误或警告。
- 遗留事项：沿用 v01.65 的背景图体积优化事项与 v01.59 的字体授权待确认事项。

### v01.66 — 2026-07-29

- 修改范围：续滑换页动画结束后的静止状态。
- 具体变化：修复章节 `is-entering` 动画完成并切换为 `is-idle` 时，通用 `.portfolio-page-view` 入场动画被重新匹配，导致内容像重刷一样闪一帧的问题。为 `is-idle` 增加明确的 `animation: none` 和 `will-change: auto`，章节进入只播放一次，完成后直接保持最终不透明度和位置；未修改滚动阈值、换页方向、Hash 或页面内容。
- 验证结果：`npm.cmd run build` 与 `git diff --check` 通过；浏览器桌面端复测“关于 → 经历”，完成后为 `is-idle`、`animation-name: none`、`opacity: 1`、`transform: none`，滚动位置为 0；390×844 目标视口复测“联系 → 首页”得到相同静止状态且无横向溢出，控制台无错误或警告。
- 遗留事项：沿用 v01.65 的背景图体积优化事项与 v01.59 的字体授权待确认事项。

### v01.65 — 2026-07-29

- 修改范围：联系页背景素材、无框排版与求职收尾文案。
- 具体变化：使用用户提供的绿色草地与流动光影图片作为联系页全屏背景，素材保存为 `public/assets/contact-green-light.png`；移除上一版参考海报延伸出的网格底纹、矩形色块和信息框体，只保留用于保证文字对比度的透明森林绿遮罩。删除“项目与合作”及“如果你想了解我的完整作品、平台链接或具体项目复盘，可以通过邮件联系我。”整块内容，避免把已经完整的求职作品集表达成仍需额外索取材料；收尾内容精简为主标题与“求职联系”邮箱，邮箱改为透明底、无外框、仅底部分隔线的文字入口。移动端标题字号略微收紧并锁定三段单行，保证“可见、可信，也可转化。”不拆字。
- 验证结果：`npm.cmd run build` 与 `git diff --check` 通过；浏览器确认背景图成功加载且自然尺寸为 736×1104，已删除的说明文字不再存在，邮箱背景透明、圆角为 0、触控高度 58px。桌面目标视口下图片全幅覆盖、文字层级清晰；390×844 目标视口下三段标题分别保持单行，标题内容宽 413px，页面无横向溢出，控制台无错误或警告。
- 遗留事项：背景图原文件为约 1.03MB PNG；当前环境没有图像转换器，后续可在不改变画面的前提下转为高质量 WebP 以进一步减小传输体积。另沿用 v01.59 的字体授权待确认事项。

### v01.64 — 2026-07-29

- 修改范围：续滑切换进入新章节时的首帧定位与空间动效。
- 具体变化：移除新章节本体 `translateY(100vh) → 0` 的整屏位移，避免经历页视觉上仍像从整页最底部快速滚到顶部；保留旧章节向上退出和纸面背景承接，新章节改为在顶部正确位置从 42px 下方轻量显现。新增基于活动章节的 `useLayoutEffect` 首帧回顶，在浏览器绘制新章节前将滚动位置校正为 0，防止渲染后再回顶；低动态覆盖继续关闭所有章节位移。
- 验证结果：`npm.cmd run build` 与 `git diff --check` 通过；浏览器再次复测“关于 → 经历”：关于页初始位置约为 0、页底约为 1892，旧页使用 `portfolioChapterExit` 向上退出；经历页切换后标题为“项目经历”、Hash 为 `#projects`、滚动位置为 0，进入完成后 transform 为单位矩阵，控制台无错误或警告。
- 遗留事项：沿用 v01.59 的字体授权待确认事项。

### v01.63 — 2026-07-29

- 修改范围：关于页续滑进入经历页时的章节定位与进入动效。
- 具体变化：修复续滑换页仍通过 `window.location.hash` 触发浏览器原生锚点定位，导致新章节出现额外“从底部快速滚到顶部”的问题。页底续滑现在使用 History API 写入新 Hash，并由 React 在旧页退出完成后直接切换活动章节和一次性归零滚动位置，不再产生原生锚点滚动；新章节从下方进入的时长由 700ms 调整为 880ms，并使用更平缓的空间缓动。顶部菜单、URL Hash、刷新直达和浏览器前进后退保持可用。
- 验证结果：`npm.cmd run build` 与 `git diff --check` 通过；浏览器针对“关于 → 经历”链路复测：关于页在 `scrollY ≈ 1892` 显示续滑入口，再次下滑后地址变为 `#projects`，经历页标题正确、最终滚动位置稳定为 0、过渡状态正常结束；浏览器返回可回到 `#about`，控制台无错误或警告。
- 遗留事项：沿用 v01.59 的字体授权待确认事项。

### v01.62 — 2026-07-29

- 修改范围：五个独立视图的页底续滑导航与章节换页动效。
- 具体变化：页底入口统一放在内容底部中央并默认隐藏；长页面到达真实页底后显现，短页面首次继续下滑时显现，提示文字统一为“继续向下滑动，进入下一部分”，联系页对应提示返回首页。提示显现后须结束第一段滚动并再次向下滚动（滚轮累计约 84px）或再次上滑（触控位移约 56px）才触发，避免一次惯性滚动误跳；链接仍支持点击和键盘。章节触发后不再让画面从旧页底部倒卷回顶部，而是让旧内容整体向上退出、短暂露出自然纸面背景，再让新章节从视口下方接续进入；URL Hash、浏览器历史和独立页面结构保持不变，低动态模式直接切换并关闭整屏位移。
- 验证结果：`npm.cmd run build` 与 `git diff --check` 通过；浏览器在桌面目标视口验证项目页入口初始隐藏、页底显现并水平居中（中心偏差约 9px）、第一段滚动停留在 `#projects`、第二段滚动进入 `#strengths`；在 390×844 目标视口验证短联系页首次下滑只显示 310×134px 居中提示、再次下滑返回 `#home`，页面无横向溢出。章节退出状态使用 `portfolioChapterExit`、进入状态使用 `portfolioChapterEnter`，换页后滚动位置为 0；低动态覆盖规则存在，控制台无错误或警告。
- 遗留事项：沿用 v01.59 的字体授权待确认事项。

### v01.61 — 2026-07-29

- 修改范围：五个独立视图的页底连续浏览入口。
- 具体变化：将原先持续可见且靠边的章节入口改为页底居中、默认隐藏；长页面滚动到真实底部后显示，短页面在用户继续向下滚动时显示。入口加入下一部分名称与“继续向下滑动，进入下一部分”提示，联系页显示返回首页提示；显现前不进入键盘顺序，显现后保留语义化链接、44px 以上触控范围、点击与键盘操作。滚轮和触控仅在页底监听明确的第二段向下意图，不做全页滚动劫持。
- 验证结果：`npm.cmd run build` 与 `git diff --check` 通过；浏览器桌面端确认项目页入口初始 `aria-hidden=true` 且不可见，到达底部后 `aria-hidden=false` 并居中显示；移动端确认入口宽 310px、高 134px、无横向溢出，控制台无错误或警告。
- 遗留事项：沿用 v01.59 的字体授权待确认事项。

### v01.60 — 2026-07-29

- 修改范围：联系页标题排版、信息结构与背景配色。
- 具体变化：参考用户提供的模块化网格海报，将联系页重构为“章节眉题与页码 → 三段主标题 → 项目说明 / 邮件入口双栏 → 返回首页”的纵向编辑结构；标题桌面行高提升至约 1.24，手机端提升至约 1.34，正文行高提升至约 1.95。背景从原深墨绿改为首页森林照片提取色的浅色延展：以树冠雾绿 `#5f7068`、中层森林 `#44564f` 和深部树影 `#1d3129` 为色相依据，提高明度形成浅雾绿纸面，并用细网格与克制矩形色块建立层级；未生成新图片或新增依赖。
- 验证结果：`npm.cmd run build` 与 `git diff --check` 通过；浏览器在 1440×900、390×844 和 375×667 目标视口检查，桌面主标题稳定为三段，手机端标题与信息区自然单列；邮件入口高度 58px、返回首页入口不低于 82px，页面无横向溢出，控制台无错误或警告。
- 遗留事项：沿用 v01.59 的字体授权待确认事项。

### v01.59 — 2026-07-29

- 修改范围：首页中文品牌标题字体、五个独立视图的连续浏览入口。
- 具体变化：首页“折椿”由 Zhi Mang Xing 更换为用户指定的 `073 TEGUSE — Kanaka Font（鹿仲茉菜の手書）`，通过 ZeoSeven FontsAPI 按字符子集加载并保留本机楷体回退，不把字体源文件写入仓库；首页、关于、经历、优势、联系页底部新增统一的章节指示，前四页分别前往下一页，联系页提供返回首页入口。入口使用 Lucide 线性箭头、语义化链接、44px 以上触控区域和轻量方向反馈，不增加滚动劫持。
- 验证结果：`npm.cmd run build` 与 `git diff --check` 通过；浏览器确认 073 TEGUSE 字体实际加载，1440×900、390×844 与 375×667 目标视口下标题和五页入口无横向溢出，首页→关于及联系→首页 Hash 跳转正常；五页入口高度均不低于 52px，控制台无错误或警告，低动态规则会停用箭头循环位移。
- 遗留事项：字体作者将 Web Font 嵌入标记为需咨询；正式公开发布前应取得作者许可，或改为已明确允许网页嵌入的近似手写字体。

### v01.58 — 2026-07-29

- 修改范围：关于页主标题换行。
- 具体变化：将主标题拆分为不可拆行的两段，固定显示为第一行“偏内容创意型的”、第二行“新媒体运营。”，避免“新”字落在上一行末尾；字体、字号和原有内容保持不变。
- 验证结果：`npm.cmd run build` 与 `git diff --check` 通过；浏览器在 1038×830 桌面视口与 390×844 手机视口检查，标题均按指定语义分为两行，页面无明显横向溢出，控制台无错误或警告。
- 遗留事项：无。

### v01.57 — 2026-07-29

- 修改范围：首页中文品牌标题字体及字体加载回退。
- 具体变化：根据用户提供的现代写意软笔手写体参考，仅将首页“折椿”更换为 Google Fonts 官方的 Zhi Mang Xing（志莽行书）；新增 `--font-handwriting` Token，并配置 STXingkai、Xingkai SC 等系统回退。中文标题使用 400 字重、轻微横向舒展和约 -1.2° 倾斜，英文 `Editorial Content Portfolio`、正文和其他页面字体保持不变；低动态设置下取消标题倾斜。未把参考截图作为页面图片使用，也未生成新素材或引入 npm 依赖。
- 验证结果：`npm.cmd run build` 与 `git diff --check` 通过；浏览器确认“折椿”对应的 Zhi Mang Xing 中文字形加载成功，桌面端标题约 81px、移动端约 59px，英文副标题仍使用 EB Garamond。390 目标视口下中文标题保持单行，标题、英文副标题与封面内容无横向溢出；控制台无错误或警告。
- 遗留事项：无。

### v01.56 — 2026-07-29

- 修改范围：关于页人物照片、求职方向信息与职业履历之间的版式关系。
- 具体变化：移除人物照片最大 520px 居中的孤立卡片布局，将人物模块改为 1120px 横向编辑式档案；桌面端照片靠左并控制在约 410px，右侧使用约 612px 信息场承载 Profile 编号、邮件、求职方向、能力切换与说明，中间以细线和留白连接。取消该模块的毛玻璃底面、大圆角和阴影；移动端改为左对齐 4:3 照片与略向右错位的信息区，不再出现大图正中悬置。三个方向标签统一为至少 44px 触控高度，并保留原有指针反馈、键盘语义和低动态覆盖。
- 验证结果：`npm.cmd run build` 与 `git diff --check` 通过；浏览器桌面端实测人物档案宽 1120px，照片约 410×526px、信息区约 612×526px，照片明确靠左且页面无横向溢出；390 与 375 目标视口下照片宽约 326–340px、信息区宽约 349–364px，采用左图与右移信息的错位单列，无横向溢出。运营 / 策划 / 产品标签触控高度均约 46px，策划标签点击后 `aria-selected`、焦点与说明文字同步更新；控制台无错误或警告，低动态媒体规则覆盖新增标签线与内容切换动画。
- 遗留事项：无。

### v01.55 — 2026-07-29

- 修改范围：全站编辑式字体与动效系统、独立页标题语言、导航状态、经历页两级项目总览及无障碍入口。
- 具体变化：建立 EB Garamond 与中文宋体混排 Token，并从 Google Fonts 官方来源加载英文 400 / 500 字重；新增统一的 250 / 450 / 700ms 动效 Token与 editorial easing；关于、经历、优势和联系页加入低透明度英文水印标题结构，导航当前项改为约 5px 小圆点。经历页将前 3 个项目重构为三列 3:4 精选图版，剩余 6 个项目重构为带罗马编号、摘要与类型的横向编目；760px 以下全部回到单列并取消同组淡化。保留项目数据、排序、详情窗口、轮播、灯箱、原始作品链接及 Hash 逻辑；新增“跳到主要内容”键盘入口，未新增图片和第三方 npm 依赖。
- 验证结果：`npm.cmd run build` 与 `git diff --check` 通过；浏览器桌面端实测项目母版为 1120px，精选图版三列各约 351×468px、编目 6 行，EB Garamond 加载成功；移动端断点实测精选项目单列、正文 15px、编目最小高度 44px，375 / 390 目标视口均无横向溢出。首页、关于、经历、优势、联系五个 Hash 视图、移动菜单、当前导航、浏览器返回、精选与编目项目点击、案例关闭焦点恢复、轮播、灯箱和 13 条品牌原始作品链接均正常；控制台无错误或警告。浏览器系统当前为 `no-preference`，已核对 22 组 `prefers-reduced-motion` 媒体规则，新增页面与项目动效均包含关闭位移、缩放、延迟和分组淡化的覆盖。
- 遗留事项：无。

### v01.54 — 2026-07-29

- 修改范围：优势页信息架构与独立内容页非标题文字层级。
- 具体变化：优势页由六步横排加三列能力矩阵，重构为“工作方法 / 核心能力”双栏编辑结构；桌面端两栏各自纵向展开，移动端顺序叠放。统一独立页面正文、条目说明、元信息和眉题编号四级字号，优势页正文由原约 10.5–12px 提升到 14–16px，并补充页面导语。
- 验证结果：`npm.cmd run build` 与 `git diff --check` 通过；桌面端浏览器实测优势页为 427px / 589px 双栏，流程与能力各自单列，正文为 14.4–16px、标签与编号为 12px；移动端两栏顺序折叠为完整单列，正文为 14–15px，页面无横向溢出。关于页抽查正文 16px、职责说明 14.4px、邮箱与时间等元信息 13px、标签 12px；控制台无错误或警告，并确认低动态设置关闭新增位移反馈。
- 遗留事项：无。

### v01.53 — 2026-07-29

- 修改范围：独立页面版式、关于页与经历页纵向信息架构。
- 具体变化：压缩独立页面顶部留白并统一标题节奏；关于页改为“简介 → 人物与求职方向 → 职业履历”的单列纵向叙事，履历内部按编号时间、岗位公司、职责成果向下排列；经历页由多列项目矩阵改为九项单列目录，桌面采用左图右文整行索引，移动端采用图片在上、说明在下。保留项目详情窗口和现有素材，未新增位图或第三方依赖。
- 验证结果：`npm.cmd run build` 与 `git diff --check` 通过；浏览器实测桌面端关于页为 980px 单列、经历页九项均为 1040px 单列索引，移动端两页均回到完整单列且无横向溢出；项目详情窗口可正常打开和关闭，背景滚动锁定正常；控制台无错误或警告，并确认低动态设置会停用新增位移反馈。
- 遗留事项：无。

### v01.52 — 2026-07-28

- 修改范围：全站一级信息架构、顶部导航与页面切换体验。
- 具体变化：将原本连续向下浏览的五个主模块改为菜单式独立视图；顶部“首页 / 关于 / 经历 / 优势 / 联系”现在只打开对应内容，URL Hash 支持直接访问、刷新保留和浏览器前进后退；切换时自动回到顶部，并加入克制的淡入位移动画，低动态设置下停用。保留现有项目详情窗口、内容、自然系视觉和素材，不新增位图。
- 验证结果：`npm.cmd run build` 与 `git diff --check` 通过；浏览器实测桌面端五个菜单项均只渲染对应主模块，URL Hash、当前项高亮、切页回顶和浏览器返回正常；项目详情窗口可打开和关闭；移动端菜单可展开、选页后自动收起，页面无横向溢出，控制台无错误或警告；已确认 `prefers-reduced-motion` 下关闭换页动画的样式规则生效条件完整。
- 遗留事项：无。

### v01.51 — 2026-07-28

- 修改范围：全站基础色彩系统与作品链接强调色。
- 具体变化：将主色统一为深苔墨绿 `#23483D`，页面底色调整为暖米纸白 `#F5F1E8`，并以雾蓝灰 `#7D9BA4` 取代旧金色在进度、分隔线、编号与作品链接索引中的强调角色；保留现有布局、内容和自然素材，不新增位图。
- 验证结果：`npm.cmd run build` 与 `git diff --check` 通过；浏览器实测桌面与移动端首屏色彩、导航和“查看核心案例”锚点跳转正常，移动端无横向溢出，控制台无错误或警告，低动态样式规则仍存在。
- 遗留事项：无。

### v01.50 — 2026-07-27

- 修改范围：项目详情窗口的原始作品链接横向布局与手机端可达性。
- 具体变化：平台信息保持左侧索引，右侧作品链接根据实际宽度自动横向分列；空间不足时自动减少列数，不再将中文标题压成逐字竖排。含作品链接的详情窗口明确始终启用内部纵向滚动，并显示细滚动条提示，手机端链接改为单列以保证完整阅读与触控。
- 验证结果：`npm.cmd run build` 与 `git diff --check` 通过；浏览器实测桌面端平台轨道为 176px、作品区自动分为三列（每列约 272px），手机端改为单列（约 401px），均无页面横向溢出；手机弹窗滚动容器为 `overflow-y: auto`，内容高 1525px、可视高 925px，实测下滑后滚动位置达到约 600px；控制台无错误或警告。
- 遗留事项：无。

### v01.49 — 2026-07-27

- 修改范围：项目详情窗口原始作品链接的横向分组版式。
- 具体变化：恢复桌面与平板端两列横向作品索引，平台分组并列展示；手机端保留单列以保证文字与触控区域可读。详情窗口继续支持内部纵向滚动，因此所有链接仍可完整查看与跳转。
- 验证结果：`npm.cmd run build` 与 `git diff --check` 通过；浏览器实测链接区为两列并列（每列约 508px），详情滚动容器内容高度为 1364px、`overflow-y` 为 `auto`，页面无横向溢出，控制台无错误或警告。
- 遗留事项：无。

### v01.48 — 2026-07-27

- 修改范围：项目详情窗口的作品链接可达性与内部滚动。
- 具体变化：修正“默认单屏摘要”样式覆盖滚动容器的问题；包含原始作品链接的详情窗口现在始终允许在窗口内部纵向滚动，用户无需展开“完整介绍”即可查看全部链接并跳转原始作品。背景页面继续锁定，滚动边界不向页面外传递。
- 验证结果：`npm.cmd run build` 与 `git diff --check` 通过；浏览器实测含作品链接的详情窗口滚动容器可视高度为 709px、内容高度为 1233px，`overflow-y` 为 `auto`，无横向溢出或控制台错误；链接保留原始地址与新窗口打开属性。
- 遗留事项：无。

### v01.47 — 2026-07-27

- 修改范围：项目经历区“不牛马厨房”独立产品实践卡与上方案例矩阵的间距。
- 具体变化：在桌面、平板和手机端为该全宽附录卡增加 16–24px 顶部留白，使其与上方展示区形成清晰分组和更舒展的阅读节奏；卡片尺寸、内容、交互和既有响应式网格保持不变。
- 验证结果：`npm.cmd run build` 与 `git diff --check` 通过；浏览器实测桌面端附录卡与上一行的实际间距为约 37px，页面无横向溢出，控制台无错误或警告；窄屏规则固定保留 16px 的新增顶部留白。
- 遗留事项：无。

### v01.46 — 2026-07-27

- 修改范围：项目详情窗口的原始作品链接信息层级。
- 具体变化：将多条原始作品链接从右侧项目摘要栏移至主图与摘要之后的独立全宽索引区；右栏只保留概述与成果数据，链接区恢复清晰的平台轨道与三列内容场，窄屏切为平台标题在上、两列作品，消除拥挤和视觉裁切感。
- 验证结果：`npm.cmd run build` 与 `git diff --check` 通过；浏览器实测 376px 窄屏下弹窗宽 360px、页面无横向溢出，链接区宽 318px，窗口纵向滚动已启用且可完整容纳 1311px 内容；1366px 桌面端弹窗宽 1180px、链接区宽约 1070px，所有链接无内容溢出，控制台无错误或警告。
- 遗留事项：无。

### v01.45 — 2026-07-27

- 修改范围：项目详情窗口的原始作品链接版式。
- 具体变化：为右侧项目摘要中的作品链接增加独立的窄栏布局，平台标签保留左侧轨道，作品标题由三列调整为两列并设置可读最小文字宽度；窄屏时平台标签回到上方，避免中文逐字竖排或内容越界。
- 验证结果：`npm.cmd run build` 与 `git diff --check` 通过；浏览器实测桌面端作品链接为两列，所有链接均无内容溢出，中文标题不再逐字竖排；402px 手机视口下平台标题置于上方、作品保持两列，页面无横向溢出，控制台无错误或警告。
- 遗留事项：无。

### v01.44 — 2026-07-27

- 修改范围：项目经历案例索引的阅读提示、键盘焦点与触屏反馈。
- 具体变化：在九张项目卡内增加低强调“阅读案例”路径提示；桌面端仅在悬停或键盘聚焦时显现，触屏端保持轻量可见；补充清晰的焦点描边与层级，帮助招聘者快速理解卡片可打开包含执行过程、成果数据和原始作品的案例阅读窗口。
- 验证结果：`npm.cmd run build` 与 `git diff --check` 通过；桌面端实测项目区为四列两行案例矩阵，阅读提示仅在交互状态出现，不干扰默认的编辑式信息层级；390×844 触屏视口下阅读提示已包含在项目按钮可访问名称中，首个案例可正常打开、背景滚动锁定；浏览器控制台无错误或警告。
- 遗留事项：无。

### v01.43 — 2026-07-27

- 修改范围：不牛马厨房 AI 产品案例的公开在线入口。
- 具体变化：将项目数据、域名展示文字与打开按钮统一更新为 `https://freebite.zhechun.space`。
- 验证结果：待生产构建、部署与线上链接检查。
- 遗留事项：无。

### v01.42 — 2026-07-27

- 修改范围：首页自然摄影素材的传输体积、首屏加载优先级与资源引用。
- 具体变化：将四张首屏 RGB PNG 转为质量 92 的 WebP，并替换 React 与 CSS 中的全部引用；主视觉图片增加高优先级加载。四张图片总传输体积由约 8.1MB 降至约 1.18MB（约减少 85%），在保持自然摄影细节的前提下改善首次加载速度；原 PNG 将移至本地未发布素材目录备份。
- 验证结果：待生产构建、浏览器桌面与移动端加载检查及线上部署验收。
- 遗留事项：无。

### v01.41 — 2026-07-27

- 修改范围：GitHub Pages 自定义域名的 Vite 资源路径与部署配置。
- 具体变化：将 `vite.config.js` 的 `base` 从仓库子路径 `/zhechun333/` 改为相对路径 `./`，使构建后的 JS、CSS 与公开素材同时兼容 GitHub 项目地址和 `resume.zhechun.space` 自定义域名根路径；Cloudflare DNS 配置未作改动，`resume` 记录继续使用 DNS only（灰云）。
- 验证结果：待重新构建、推送并完成线上请求验收。
- 遗留事项：无。

### v01.40 — 2026-07-27

- 修改范围：项目详情窗口的 AI 产品文案、海报版式与普通案例作品链接位置。
- 具体变化：将“不牛马厨房”改述为餐食决策工具，明确 AI 仅用于辅助制作而非用户侧功能；桌面端将海报与叙事调整为均衡双栏，并扩大海报展示尺寸；普通案例的作品链接移入主图右侧的项目摘要区，移除下方折叠内容中的重复链接，避免产生无效留白。
- 验证结果：`npm.cmd run build` 与 `git diff --check` 通过；浏览器实测桌面端海报宽 352px、叙事栏宽约 509px，标题可使用完整叙事栏且页面无横向溢出；402px 移动视口下海报宽 286px、无横向溢出，控制台无错误或警告；普通案例链接已确认位于项目摘要区，折叠内容中不再重复显示。
- 遗留事项：无。

### v01.39 — 2026-07-27

- 修改范围：根目录本地运行入口的 Windows 兼容性与端口复用判断。
- 具体变化：移除批处理文件内会被 Windows `cmd` 以错误编码解析的中文文本，改用 ASCII 提示；将端口检测改为两段 `findstr` 管道，正确识别已监听的 5174 端口并直接打开现有预览；依赖检查改为确认 Vite 可执行文件存在，避免仅有空 `node_modules` 目录时跳过安装。
- 验证结果：已复现并修正原脚本的中文文本解析错误与端口重复启动问题；实际执行入口后正确识别已监听的 5174 端口，以退出码 0 提示并打开现有预览，未再输出错误命令或重复启动 Vite；`git diff --check` 通过。
- 遗留事项：无。

### v01.38 — 2026-07-27

- 修改范围：根目录本地运行入口与维护说明。
- 具体变化：新增 `启动本地预览.bat`；双击后自动切换至项目根目录，若 5174 端口已有本地预览则直接在浏览器打开，其他情况下检查 npm 与依赖目录，首次缺少依赖时执行安装，再以固定的 5174 端口启动 Vite 并自动打开本地预览。同步在项目概述中补充使用方式与停止服务说明。
- 验证结果：批处理语法与实际 npm 开发命令核对完成；待在 Windows 资源管理器中双击进行一次人工启动确认。
- 遗留事项：无。

### v01.37 — 2026-07-27

- 修改范围：AI 产品案例主区的海报位置、左右栏比例与响应式衔接。
- 具体变化：将桌面端海报从左栏起点对齐改为独立视觉栏内水平、垂直居中；调整主区为更协调的 0.78 / 1.22 非对称双栏和自适应栏间距，海报宽度随视口在 232–276px 间变化，使其视觉中点与右侧标题、说明、数据和操作区的整体重心对应；760px 以下继续使用居中单栏，并解除桌面端拉伸规则，保持原有自然阅读顺序。未新增素材或依赖。
- 验证结果：生产构建与 `git diff --check` 通过；浏览器桌面端实测主区约为 395 / 617px 双栏，海报在左栏内水平居中，且海报、左栏和右侧叙事区的中心纵坐标一致；402×874 手机视口下海报与单栏内容中心线一致，页面和弹窗均无实际横向溢出；桌面与手机控制台均无错误或警告。现有 `prefers-reduced-motion` 规则保持有效，本次未新增动画。
- 遗留事项：无。

### v01.35 — 2026-07-27

- 修改范围：AI 产品案例的海报呈现、产品命题排版和产品价值表达。
- 具体变化：移除海报外层的白色展示框、内边距、毛玻璃与大圆角，海报按原始比例直接完整显示并在桌面端决定左栏高度，右侧叙事随海报高度居中适配；将过大的产品命题收敛为更紧凑的平衡换行，并改写为“把今晚吃什么，做成真正能用的 AI 点餐产品”；删除“甲状腺模式”、减肥和地域口味等标签，将价值区重构为“核心痛点 / 核心功能 / 产品特色”三组，分别强调选择困难、800 道家常菜与随机转盘、偏好筛选与转盘宠物反馈；760px 以下使用无框完整海报和三行编辑式价值索引。
- 验证结果：待完成生产构建与 1440×900、1366×768、584×831、402×874 浏览器验收后补充。
- 遗留事项：无。

### v01.34 — 2026-07-27

- 修改范围：第 09 项“不牛马厨房”案例窗口的信息架构、主内容顺序与响应式排版。
- 具体变化：取消 AI 产品案例的“普通摘要 + 完整介绍折叠页”结构，将原折叠页中的产品命题、实现说明、多场景体验、在线入口和原创 Skill 全部提升为默认主内容；把原主内容中的项目标题、状态、完整纵向海报、项目概述和三项数据融入同一产品案例版面；桌面端使用海报与产品叙事双栏、实现与场景双栏，760px 以下改为连续单栏；AI 产品窗口默认允许纵向滚动，不再依赖展开状态；其他八个运营案例的摘要与折叠结构保持不变。
- 验证结果：生产构建与 `git diff --check` 通过；1440×900 下 AI 产品窗口打开后直接显示完整产品案例，无折叠条，海报与叙事主区约 1070×516px，海报完整落在约 397×450px 主图区内，窗口内容约 991px 高并启用自然纵向滚动；1366×768 下双栏约为 397 / 621px，内容约 983px 高；584×831 与 iPhone 16 Pro 的 402×874 视口下主区和产品实现区均切为单栏，海报、产品命题、三项数据、在线入口、Skill、实现说明与八个场景按顺序完整显示，三项数据标签不再被普通案例规则压缩；四种视口均无实际横向溢出，关闭按钮与外链正常，控制台无错误或警告；回归抽查品牌项目仍保留默认摘要和关闭状态的“完整介绍与作品链接”折叠层。
- 遗留事项：无。

### v01.33 — 2026-07-27

- 修改范围：第 09 项“不牛马厨房”总览卡片在平板、窄屏和手机上的文字层级与内部位置。
- 具体变化：将特殊横向卡片的图片区与文字区比例调整为更稳定的 45 / 55，并以细竖线和轻纸面渐变明确右侧说明区；类型标签改为小号绿色标识，项目名加大并收紧行高，`800 · 道家常菜` 改为带短分隔线的成果脚注；统一三行左基线、垂直居中和箭头安全边距；520px 以下进一步收紧字号与内边距；未修改项目内容、图片、点击交互或桌面端 4×2 项目矩阵。
- 验证结果：生产构建与 `git diff --check` 通过；584×831 下特殊卡约 530×130px，图片区与文字区约为 237 / 290px，三层文字总高约 70px 并在 128px 文字区内上下各保留约 29px，类型、标题和成果脚注均为单行；iPhone 16 Pro 的 402×874 视口下卡片约 349×130px，文字区约 194×128px，三层文字继续保持单行且无横向溢出；1366×768 下桌面附录仍为约 1212×176px，未发生布局回退；箭头完整位于安全边距内，控制台无错误或警告，低动态规则继续取消卡片反馈位移。
- 遗留事项：无。

### v01.32 — 2026-07-27

- 修改范围：仅含一个外部链接的案例在完整材料区中的专用入口样式。
- 具体变化：为单链接状态增加独立渲染分支，不再显示“作品索引”、平台编号、作品数量和单行档案表；第 09 项改为横向在线产品入口，直接展示“在线产品”、项目名与域名，并使用带外链箭头的深绿色“打开在线体验”按钮；520px 以下自动改为上下排列和满宽按钮；多链接项目继续使用原有横向杂志索引，链接地址与安全属性不变。
- 验证结果：生产构建与 `git diff --check` 通过；1366×768 与 1440×900 下单链接入口均约 1082×112px，主按钮为 176×48px，项目名、域名和操作文案完整显示；iPhone 16 Pro 的 402×874 视口下入口自动收敛为约 357×133px 单列结构，按钮约 329×48px 且无横向溢出；链接仍指向 `https://zhechun.space`，保留 `_blank`、`noopener` 与 `noreferrer`；回归抽查品牌项目仍显示 13 条作品、4 个平台的原始索引；控制台无错误或警告，低动态规则会取消按钮与箭头位移。
- 遗留事项：无。

### v01.31 — 2026-07-27

- 修改范围：第 09 项“不牛马厨房”单张纵向主图在案例弹窗中的跨设备缩放方式。
- 具体变化：修复单图主图区只有最大高度、图片却继续按 520px 宽度计算纵向高度而被外层裁切的问题；将单图主图区改为明确的 `50dvh / 450px` 响应式高度，手机端使用 `39dvh / 276px`，图片同时受容器宽高限制并以 `object-fit: contain` 保持比例完整居中；不拉伸、不裁切、不替换原图，也不影响多图轮播。
- 验证结果：生产构建与 `git diff --check` 通过；1366×768 下主图区约 651×384px，原图由修复前的 520×631px 裁切状态收敛为约 186×328px 完整显示；1440×900 下主图区约 650×450px、图片约 223×394px；iPhone 16 Pro 的 402×874 视口下主图区约 357×276px、图片约 147×260px；三种视口下图片宽高比均与原图约 0.563 的比例一致，完整落在容器内且无横向溢出，关闭按钮正常，控制台无错误或警告。
- 遗留事项：无。

### v01.30 — 2026-07-27

- 修改范围：案例弹窗展开区中 AI 视觉与 AI 产品补充模块的显示状态。
- 具体变化：定位到大面积空白并非作品索引或弹窗高度造成，而是弹窗动态创建的补充模块复用了仅在页面初始化时注册的 `reveal-on-scroll` 类，导致模块保留约 287px 布局高度却保持透明；移除两个弹窗专用组件的页面级滚动显现类，使原始作品链接结束后立即衔接实际补充内容；未修改作品链接、图片、文案、弹窗尺寸或主页面入场动画。
- 验证结果：生产构建与 `git diff --check` 通过；1440×900 下将作品索引滚动至窗口顶部后，链接区约 419px 高，后续 AI 视觉模块以 `opacity: 1`、无位移状态紧接显示，模块约 287px 高，两章之间保持 46px 正常间距，不再出现透明占位；iPhone 16 Pro 的 402×874 视口下品牌 AI 视觉模块与第 09 项 AI 产品模块均直接可见、宽度约 357px且无横向溢出；关闭按钮完整位于安全边距内，控制台无错误或警告；主页面既有显现动画及低动态规则未改变。
- 遗留事项：无。

### v01.29 — 2026-07-27

- 修改范围：九项项目索引中第 09 项 AI 产品实践的跨设备层级，以及前八项为其让位后的整体网格。
- 具体变化：将“不牛马厨房”从普通项目卡中独立出来，在桌面、平板和手机上都固定为最后一项并独占完整一行；常见笔记本端将前八项岗位直接相关案例由三列九宫格改为 4×2 等高矩阵，彻底消除八项内容在三列布局中留下的空位；第 09 项在桌面端改为 176px 高的横向 AI 产品附录，图片区与说明区左右并排，标题层级同步放大；平板和手机继续使用前八项 2×4、末项横跨两列的结构；未修改项目顺序、图片、文案、数据或弹窗交互。
- 验证结果：1440×900 下前八项为四列两行，单卡约 294×228px，第 09 项为约 1216×176px 的独立横行；1366×768 下前八项仍为四列两行，单卡约 293×228px，第 09 项约 1212×176px；iPhone 16 Pro 的 402×874 视口下前八项为 170×184px 的两列四行，第 09 项为 349×130px 的独立横行；三种视口均无横向溢出，项目顺序保持 01–09，浏览器控制台无错误或警告，既有 `prefers-reduced-motion` 规则继续取消卡片位移并压缩过渡。
- 遗留事项：无。

### v01.28 — 2026-07-27

- 修改范围：案例窗口原始作品链接的整体构图、平台章节关系与跨设备编辑式节奏。
- 具体变化：放弃平台双栏和等宽块状网格，将链接区重构为横向杂志索引；521px 以上每个平台占一条横向章节，左侧使用 118–160px 的编号、平台名和作品数标签轨道，右侧作品按三列流动，内容多的平台自然占两行、内容少的平台占一行，变化由内容量决定而非随机设定；520px 以下将平台标题移至作品上方并使用两列，保留手机可读性；平台之间仅用细分隔线和留白分章，不增加固定大小卡片、背景框或装饰动画；未修改链接内容、安全属性或项目数据。
- 验证结果：1440×900 下各平台章节使用 160px 标签轨道与右侧三列作品索引，品牌案例 13 件作品的链接区约 419px 高；584×830 下标签轨道收敛为 118px，作品区继续保持三列，四个平台章节高度按内容自然形成 82 / 132 / 78 / 82px；iPhone 16 Pro 的 402×874 视口下平台标题移至上方，作品自动切换为两个约 172px 的等宽列，链接区约 598px 高；三种视口均无内部横向溢出，关闭按钮完整位于弹窗内，浏览器控制台无错误或警告。
- 遗留事项：无。

### v01.27 — 2026-07-27

- 修改范围：案例窗口原始作品链接在手机与窄屏弹窗中的横向空间利用和纵向篇幅。
- 具体变化：保留作品档案抬头和平台分组，但取消手机端“每条链接独占一整行”的纵向列表；680px 以下每个平台标题继续横跨整行，组内作品改为两列索引，并收紧组间距、序号列、箭头列和单条内边距；品牌项目 13 条链接由 13 个纵向作品行压缩为 8 个横向行，不采用瀑布流、不跨平台混排；未修改链接地址、作品名称、外链安全属性或桌面端双栏结构。
- 验证结果：生产构建与 `git diff --check` 通过；584×830 下各平台组内形成两个约 262px 的等宽作品列，13 条链接按 2 / 3 / 1 / 2 行排布，链接区约 588px 高，所有标题无截断、无内部溢出，页面无横向滚动；iPhone 16 Pro 的 402×874 视口下作品列约 171px，仍按 8 个横向行排布，长标题按需换行、单条最大约 51px，链接区约 602px 高且无横向溢出；1440×900 下继续保持两个约 514px 的平台栏和组内单列作品行，未发生桌面布局回退；控制台无错误或警告，既有低动态规则继续覆盖链接反馈。
- 遗留事项：无。

### v01.26 — 2026-07-27

- 修改范围：关于区人物面板与七条职业履历在笔记本端的高度关系、行密度与摘要排版。
- 具体变化：不再让右侧履历的累计内容高度撑开双栏；1100px 以上由左侧人物面板的真实高度决定整行高度，右侧履历使用尺寸隔离并将七条记录等分填入相同高度，使两栏顶部和底部精确对齐；同步将履历单条上下留白由 14px 收紧为 8px、内容间距收紧为 3px，摘要在宽屏保持单行省略；1100px 以下继续使用原有自然高度与响应式规则，不为手机端强行等高；未删除岗位、公司、类型、时间、职责或成果内容，未修改人物图片和交互。
- 验证结果：生产构建与 `git diff --check` 通过；1440×900 与 1366×768 下左侧人物面板和右侧履历均为 658px 高，顶部、底部坐标完全一致，七条履历均分为约 94px，七条职责/成果摘要全部可见且未发生文本溢出，页面无横向溢出；iPhone 16 Pro 的 402×874 视口下未启用等高约束，人物面板约 471px、履历按内容自然展开约 733px，七条摘要全部可见、允许自然换行且无横向溢出；控制台无错误或警告；本次没有新增动效，既有低动态规则保持不变。
- 遗留事项：无。

### v01.25 — 2026-07-27

- 修改范围：九项案例完整材料中的原始作品链接结构、排版、交互反馈与响应式规则。
- 具体变化：将原来的大圆角玻璃链接框和不等高瀑布分组完全重构为编辑式作品档案；新增“作品索引”抬头、作品/平台数量、双位平台编号、分组作品数与作品行序号；桌面端使用严格对齐的双栏平台章节，手机端统一为连续单栏，取消基于链接数量估算网格行高的内联样式；每条作品行保持至少 44px 点击区域，以细分隔线组织密度，并加入克制的纸面显色、外链箭头位移、按压和键盘焦点反馈；未修改任何链接地址、作品名称、项目顺序或视觉素材。
- 验证结果：生产构建与 `git diff --check` 通过；在品牌自媒体案例中实测 13 件作品、4 个平台均完整保留，1440×900 与 1366×768 下平台章节为两个 514px 等宽栏、作品行最小高度 44px；iPhone 16 Pro 的 402×874 视口下自动切换为 357px 单栏，作品行最小高度 46px；另抽查 3 件作品、1 个平台的案例，数量与单栏章节均正确收敛；三种视口均无横向溢出，外链仍使用 `_blank`、`noopener` 和 `noreferrer`，控制台无错误或警告；低动态规则会将链接和箭头过渡压缩至 0.01ms 并取消位移。
- 遗留事项：无。

### v01.24 — 2026-07-27

- 修改范围：九项项目索引的网格规律、卡片尺寸、图片比例与响应式一致性。
- 具体变化：取消桌面端循环使用 5 / 4 / 3、4 / 5 / 3 列宽的非等分规则，改为严格的三列九宫格；九张卡片统一为 240px 高、图片区统一为 126px，并同步统一圆角、正文区垂直位置和标题字级；桌面端第 09 项恢复与其他项目一致的实线边框和尺寸；平板与手机保持双列，前八项固定等高，第 09 项继续横跨两列作为有明确语义的横向附录项；未修改项目顺序、图片、文字、弹窗交互或动效。
- 验证结果：生产构建与 `git diff --check` 通过；1440×900 下三列均为 396px，1366×768 下三列约为 395px，九张卡片均为 240px 高、图片区均为 126px，三行各三项且无横向溢出；iPhone 16 Pro 的 402×874 视口下，前八项为 170px 宽、184px 高的 2×4 等高矩阵，第 09 项为 349px 宽、130px 高的跨列收尾卡片，同样无横向溢出；实测项目卡片可正常打开和关闭弹窗，控制台无错误或警告；既有 `prefers-reduced-motion` 规则仍会取消卡片位移并将过渡压缩至 0.01ms。
- 遗留事项：无。

### v01.23 — 2026-07-27

- 修改范围：项目经历章节标题的内容层级、字级与手机端间距。
- 具体变化：删除重复的“项目经历”眉题、“代表项目”大标题和岗位相关度说明段落，章节只保留一个“项目经历”标题；标题采用克制的编辑式字级、单行排版和一条细分隔线，并收紧与九项案例索引之间的距离；不增加英文编号、胶囊、卡片或装饰性动效。
- 验证结果：生产构建通过；1440×900 与 1366×768 下标题区均只包含“项目经历”一个标题，宽度与 1216px 项目母版一致，标题到项目索引的旧版 72px 留白压缩为 32px；iPhone 16 Pro 的 402×874 视口下标题区约 349×53px，标题到索引间距 18px，九项项目仍保持双列；三种视口均无横向溢出，控制台无错误或警告；本次未新增动效，既有低动态行为不变。
- 遗留事项：无。

### v01.22 — 2026-07-27

- 修改范围：关于区职业履历的招聘价值表达、摘要可见性与手机端信息层级。
- 具体变化：将原来容易被历史 `p:last-child` 规则隐藏的摘要段落改为独立信息行，确保七段经历均明确显示；在公司名下增加克制的“职责 / 成果”文字标识，并将内容收敛为品牌内容统筹、账号矩阵搭建、30+ 线索、团队前三及银牌、热点第 1 与 10W+ 阅读、37 城内容供给及 100W+ 曝光、手游稿件 80W+ 阅读等招聘者可快速判断的事实；信息行默认一行、窄屏最多两行，不增加卡片或无效装饰。
- 验证结果：生产构建通过；1440×900 与 1366×768 下七条“职责 / 成果”信息行全部可见、保持单行且无截断，履历总高约 751px；iPhone 16 Pro 的 402×874 视口下七条信息行全部可见，前两条按内容自然换为两行、其余保持单行，单条约 100–117px、履历总高约 733px；三种视口均无横向溢出，控制台无错误或警告；既有低动态规则继续关闭履历顺序入场动画。
- 遗留事项：无。

### v01.21 — 2026-07-27

- 修改范围：关于区七段职业履历的内容密度、职责/成果摘要与响应式排版。
- 具体变化：在时间、岗位、类型和公司信息下恢复一条精炼摘要；基于现有案例事实分别保留品牌内容统筹、账号矩阵搭建、30+ 线索、团队前三及银牌、热点第 1 与 10W+ 阅读、37 城内容供给及 100W+ 曝光、手游稿件 80W+ 阅读等重点；摘要桌面尽量单行、窄屏最多两行，并移除岗位标题与公司名原有的装饰性下边距和下划线空间、收紧每行上下留白；未新增未经证实的数据、图片或交互。
- 验证结果：生产构建通过；1440×900 与 1366×768 下七条摘要均为单行、无截断，单条约 103px、履历总高约 725px；iPhone 16 Pro 的 402×874 及评论对应的 639×706 视口下七条摘要同样保持单行、无截断，单条约 100px、总高约 698px；各视口均无横向溢出，控制台无错误或警告。
- 遗留事项：无。

### v01.20 — 2026-07-27

- 修改范围：项目经历标题区、案例索引与上方照片/履历模块的内容宽度和左右轴线。
- 具体变化：取消案例索引贴边满宽表现；项目标题和九项案例网格统一使用全站内容母版，桌面最大宽度 1216px，并随视口采用 20px—72px 的自适应单侧边距；680px 以下直接沿用章节父层已有的手机安全边距，避免重复缩进，使项目区与关于区保持同一左右起止线；未修改项目内容、卡片交互、图片或动效。
- 验证结果：生产构建通过；1440×900 下照片/履历、项目标题和案例索引均为 1216px 宽且左右坐标完全一致；1366×768 下三者左右坐标一致；iPhone 16 Pro 的 402×874 视口下三者同为约 349px 宽、左右坐标一致，九个项目保持双列；三种视口均无横向溢出，控制台无错误或警告。
- 遗留事项：无。

### v01.19 — 2026-07-27

- 修改范围：案例窗口右上角关闭按钮的造型、尺寸、焦点状态与安全边距，以及 iPhone 16 Pro、常见笔记本分辨率下的单屏适配。
- 具体变化：移除原 44–48px 圆形按钮及旋转反馈，改为桌面 40px、手机 36px 的圆角方形按钮；手机端标题栏右侧边距增至 10px，避免按钮与焦点环超出窗口边界；图标缩小至 16–18px，悬停仅轻微上移；键盘聚焦时隐藏普通边框并显示单层方形焦点环，消除双圈视觉；手机端另以不可见热区将实际触控范围扩展至 44px，兼顾精致比例与可操作性；针对高度不超过 800px 的笔记本视口轻量压缩案例主图，保证 1366×768 下默认内容不产生内部滚动；减少动态效果模式下关闭按钮不发生位移或缩放。
- 验证结果：生产构建通过；以 iPhone 16 Pro 的 402×874 视口检查按钮边界、自动聚焦状态、点击关闭和横向溢出，并在 1440×900 与 1366×768 笔记本视口复核桌面比例；控制台无错误或警告。
- 遗留事项：无。

### v01.18 — 2026-07-27

- 修改范围：桌面端案例窗口项目概述与成果数据的字体、密度和布局。
- 具体变化：将桌面端右侧纵向堆叠的三项大号指标改为单行三列的数据脚注；取消指标卡圆角、玻璃底面和大号展示字体，改用细分隔线、正文无衬线字体、中等字重与约 20–26px 的数字尺度；同步将项目概述改为无底色的标签加正文结构，收紧字级与行距；平板和手机端既有布局保持不变。
- 验证结果：生产构建通过；在 1440×900 桌面视口检查三项指标横向排列、字体层级、默认窗口高度和横向溢出；在 662×882 与 464×706 复核响应式规则未回退；控制台无错误或警告。
- 遗留事项：无。

### v01.17 — 2026-07-27

- 修改范围：案例窗口标题区、项目概述与成果数据的信息结构和响应式排版。
- 具体变化：放弃原有“标签—大标题—副标题”三行堆叠结构，将案例扉页改为项目名称主区与“项目主题”侧栏的编辑式双分区；将裸放的简介段落改为带“项目概述”标签的说明区，并与成果数据组合为统一证据带；521px 至 960px 使用概述与三行紧凑指标左右并排，520px 以下使用概述单列与三项横向指标，桌面端延续主图与信息侧栏；移除数据块的重复玻璃卡片效果，改用分隔线和统一底面组织层级；未修改项目事实、链接、图片或第三方依赖。
- 验证结果：生产构建通过；在 1440×900、662×882 与 464×706 视口检查标题分区、概述、成果数据和横向溢出，三种尺寸默认案例窗口均无内部滚动；662px 下概述与指标等高为约 121px，464px 下概述约 75px、指标约 56px；控制台无错误或警告。
- 遗留事项：无。

### v01.16 — 2026-07-27

- 修改范围：项目图片灯箱的图片外区域关闭交互。
- 具体变化：将灯箱关闭判断改为排除图片与控制按钮后关闭，使深色留白、图片外框和说明区域均可点击关闭；将灯箱图片元素由撑满整个内容行改为按实际渲染尺寸收缩，解决视觉留白仍被浏览器判定为图片区域的问题；为可关闭区域增加缩小查看光标，图片与按钮保持各自光标语义；未修改轮播图片、项目数据或视觉素材。
- 验证结果：生产构建通过；在 464×706 手机视口实测点击图片本身灯箱保持打开，点击图片上方深色留白灯箱关闭，同时底层项目窗口保持打开；1440×900 页面无横向溢出；控制台无错误或警告。
- 遗留事项：无。

### v01.15 — 2026-07-27

- 修改范围：关于区邮箱入口的位置、样式与响应式表现。
- 具体变化：移除履历列表上方独立占行的邮箱按钮，将邮箱入口整合进个人照片右侧的求职方向信息面板，与“求职方向”标签组成同一标题行；改为无大面积底色的细线文字链接，保留邮件图标、可见地址、悬停、按压与键盘焦点反馈；360px 以下允许标题行纵向排列；低动态设置下压缩过渡时间；未新增图片、依赖或联系信息。
- 验证结果：生产构建通过；在 1000×700、862×706 和 390×844 视口检查窄栏、横向面板及手机竖排布局，邮箱均完整显示且旧独立入口已移除；页面无横向溢出，控制台无错误或警告。
- 遗留事项：无。

### v01.14 — 2026-07-26

- 修改范围：关于履历、项目标题与案例窗口、优势模块及小程序数据。
- 具体变化：将“不牛马厨房”菜品数由 240 更新为 800；项目区改为“项目经历 / 代表项目”的直接中文表达，移除能力胶囊、装饰性项目索引和大面积玻璃标题卡，并压缩板块上下留白；案例窗口将 `CASE STUDY` 改为“项目 06 / 09”式位置标识，统一标签、标题和正文的中文阅读层级，并用已有天眼查、学而思经历事实补充热点项目摘要；关于区履历改为只列时间、岗位、类型和公司信息的紧凑职业索引，详细职责交由项目区呈现；优势区改为紧凑流程与能力矩阵；履历新增顺序入场且支持低动态设置；未新增位图或第三方依赖。
- 验证结果：生产构建通过；在 1440×900 与 464×706 视口检查项目标题、履历索引、能力矩阵和横向溢出；手机端热点项目窗口默认内容高度等于可用高度，无内部滚动，完整摘要、三项数据和材料入口均在一屏内；弹窗打开与关闭正常，控制台无错误或警告；低动态样式会停用新增履历入场动画。
- 遗留事项：首次公开发布前继续人工复核作品截图中的账号、二维码和业务数据。

### v01.13 — 2026-07-26

- 修改范围：案例窗口完整材料的信息顺序与链接状态提示。
- 具体变化：保留全部原始作品链接数据，将展开区顺序由“完整简介—AI 补充—作品链接”调整为“完整简介—原始作品链接—AI / 产品补充”，避免较长的 AI 视觉内容把链接推到窗口深处；有外链的案例在入口标注“原始作品链接优先”，无外链案例只显示“完整介绍 / 展开阅读”，不再误示作品链接；未修改链接地址、分组、安全属性、视觉素材或第三方依赖。
- 验证结果：生产构建通过；在 558×706 手机视口中确认品牌自媒体案例展开后首先显示 13 条原始作品链接，AI 辅助视觉位于其后；旅游攻略无链接案例正确显示“完整介绍”；在 1440×900 桌面视口中四个链接分组正常排版，无横向溢出；弹窗打开、展开、关闭正常，控制台无错误或警告。
- 遗留事项：首次公开发布前继续人工复核作品截图中的账号、二维码和业务数据，并定期检查外部短链接有效性。

### v01.12 — 2026-07-26

- 修改范围：经历章节标题文案、专业信息层级与标题区视觉设计。
- 具体变化：删除“先看全貌，再进入一段经历”和弹窗关闭位置等界面操作式文案，将章节改为“代表项目 / 内容运营案例与实践”，明确案例按岗位相关度组织并展示职责范围、执行过程与结果复盘；补充品牌内容、平台运营、用户增长、项目协同四项能力索引；将小程序分类由“补充能力”调整为“独立产品实践 · AI 应用”；标题区升级为十二列不对称编辑网格，加入细线分栏、`01—09` 项目编号、克制的雾蓝光感与胶囊标签；手机端采用主动两行标题和紧凑单列布局；未新增位图、第三方依赖或装饰性动效。
- 验证结果：生产构建通过；在 1440×900、923×708 和 422×708 视口中复核标题比例、主动换行、标签布局、项目索引和横向溢出；手机端项目弹窗打开、默认单屏阅读与关闭正常；控制台无错误或警告，标题区沿用既有显现与低动态规则。
- 遗留事项：首次公开发布前继续人工复核作品截图中的账号、二维码和业务数据。

### v01.11 — 2026-07-26

- 修改范围：经历总览末项布局与经历区到优势区的板块衔接。
- 具体变化：将平板和手机端独占一行的第 09 项“不牛马厨房”由放大的普通纵向卡片改为 156px / 130px 的横向补充能力附录卡，图片与文字并排，并为 390px 以下窄屏进一步压缩至 126px；使用明确高度避免懒加载图片按原图比例撑开网格行；保留原有虚线边框、森林绿和纸张质感；同步收紧经历区底部与优势区顶部留白；未新增位图、动效或第三方依赖。
- 验证结果：生产构建通过；在 586×760、422×708、375×667 和 1440×900 视口中复核图片加载前后高度稳定、项目网格与下一板块衔接、无横向溢出；第 09 项弹窗打开和关闭正常；桌面端原非等分三列节奏保持不变；控制台无错误或警告，既有低动态规则仍覆盖卡片反馈。
- 遗留事项：首次公开发布前继续人工复核作品截图中的账号、二维码和业务数据。

### v01.10 — 2026-07-26

- 修改范围：案例窗口信息层级、单屏布局与长内容展开方式。
- 具体变化：将桌面端和手机端案例窗口重排为默认单屏摘要，统一展示项目标签、单行标题、主图、紧凑缩略图、两行简介、三项数据和完整材料入口；手机端项目标签缩小至约 12px、标题缩小至约 27px，强化两者层级；主图按视口自适应，缩略图与数据卡片同步压缩；完整简介、AI 补充内容和原始作品链接收进“完整介绍与作品链接”，仅在展开后启用内部滚动；焦点循环忽略折叠区内不可见链接并包含 `summary` 控件；未新增位图或第三方依赖。
- 验证结果：生产构建通过；在 422×708、375×667 手机视口和 1440×900 桌面视口中，默认案例摘要均完整落在窗口内、无内部滚动和横向溢出；展开完整材料后恢复纵向阅读；标题、标签、数据区和关闭交互正常；浏览器控制台无错误或警告。
- 遗留事项：首次公开发布前继续人工复核作品截图中的账号、二维码和业务数据。

### v01.09 — 2026-07-26

- 修改范围：手机端案例窗口标题与成果数据排版。
- 具体变化：将手机端案例标题调整为单行自适应字号，避免“个人账号运营”等标题断行；将三项成果数据由纵向宽松列表改为并排三列指标块，数字优先、说明置于下方，压缩无效留白；桌面端排版不变。
- 验证结果：生产构建通过；在 422×708 手机视口中复核“个人账号运营”和最长案例标题均保持单行，成果数据区高度明显缩短，窗口内部滚动正常且无横向溢出；控制台无错误或警告。
- 遗留事项：首次公开发布前继续人工复核作品截图中的账号、二维码和业务数据。

### v01.08 — 2026-07-26

- 修改范围：经历详情打开方式与弹窗交互。
- 具体变化：移除总览与详情之间的横向切换，改为在经历总览上方打开独立案例阅读窗口；新增暖白纸面窗口、森林绿半透明遮罩、右上角 48px 关闭按钮、点击窗外关闭、`Escape`、浏览器返回、背景滚动锁定、弹窗内焦点循环与关闭后卡片焦点恢复；手机端采用四周留有 8px 边界的近全屏窗口，详情在窗口内部独立纵向滚动；图片灯箱保持高于案例窗口，关闭图片时不会连带关闭案例窗口；未新增位图或第三方依赖。
- 验证结果：生产构建通过；桌面 1440×900 和手机 375×812 实测窗口尺寸、内部滚动、背景锁定、右上角关闭、点击窗外关闭、`Escape`、焦点恢复及无横向溢出均通过；控制台无错误或警告；低动态模式会将窗口与遮罩入场压缩为近乎即时。
- 遗留事项：首次公开发布前继续人工复核作品截图中的账号、二维码和业务数据。

### v01.07 — 2026-07-26

- 修改范围：移动端信息架构、履历密度、经历总览与案例详情交互。
- 具体变化：将原九个完整项目纵向列表重构为“经历总览—单案详情”双层浏览；总览采用桌面非等分编辑网格和手机双列索引，案例详情通过水平空间切换进入，支持顶部/底部返回、浏览器返回键、`Escape`、焦点恢复与滚动位置恢复；按岗位相关性重排项目，将“不牛马厨房”移至最后并标注为补充能力；压缩手机端履历卡片，隐藏次要长描述；导航名称改为“关于 / 经历”；保留现有自然系杂志视觉与截图资产，未新增位图和第三方依赖。
- 验证结果：生产构建通过；桌面 1440×900 与手机 375×812 实测无横向溢出；手机端经历总览为 9 张卡片、首个案例进入与返回、移动导航、浏览器返回、详情起点对齐、卡片焦点恢复均通过；已检查 `prefers-reduced-motion` 专用规则，低动态模式会关闭水平切换、图片缩放和按钮位移动效。
- 遗留事项：首次公开发布前继续人工复核作品截图中的账号、二维码和业务数据。

### v01.06 — 2026-07-24

- 修改范围：GitHub Pages 正式发布与线上资源路径修正。
- 具体变化：启用 GitHub Actions 作为 Pages 发布源；完成首次线上部署；将入场动画中的两处首屏背景图根路径改为由 Vite 构建处理的相对路径；在项目概述中补充线上入口与资源路径约束。
- 验证结果：Pages 工作流第 2 次运行成功；线上首页标题、导航和主要内容可访问；公开素材抽样请求返回 200；生产构建、桌面端与移动端页面验收将在本版本推送后再次执行。
- 遗留事项：等待本版本工作流完成后进行最终线上验收。

### v01.05 — 2026-07-24

- 修改范围：完整源码、素材与 Pages 部署准备。
- 具体变化：审核并准备上传 `src/App.jsx`、全部当前引用的 `public` 素材和 GitHub Pages 工作流；按用户要求保留原始 HTTP 作品短链接；同步项目概述与进度表。
- 验证结果：未发现手机号、微信号、密钥、危险 HTML 注入或不安全的新窗口权限；公开求职邮箱作为页面联系入口保留；所有 `public` 文件均被当前页面引用；生产构建通过。
- 遗留事项：推送后启用 GitHub Pages，等待 Actions 完成并验收线上页面。

### v01.04 — 2026-07-24

- 修改范围：GitHub 第三批安全上传。
- 具体变化：上传 React 入口 `src/main.jsx` 与全站样式 `src/index.css`，同步版本文档；继续保留 `src/App.jsx`、全部公开素材和 Pages 工作流在本地等待独立审核。
- 验证结果：入口与样式文本扫描未发现私人联系方式、密钥、外部脚本或本机绝对路径；生产构建成功；远程暂不启用 Pages，因此分批上传期间的依赖缺口不会触发失败部署。
- 遗留事项：下一批审核 `src/App.jsx` 中的公开邮箱、外链和个人内容；之后分组审核必要图片素材。

### v01.03 — 2026-07-24

- 修改范围：GitHub 第二批安全上传与部署路径配置。
- 具体变化：将 npm 包名改为 `zhechun333-portfolio`；将 Vite 基础路径从旧路径调整为 `/zhechun333/`；同步项目概述；第二批仅上传 `package.json`、`package-lock.json`、`index.html`、`vite.config.js` 和本次文档更新。
- 验证结果：生产构建成功；第二批不包含页面源码、联系邮箱、作品截图、媒体素材或 GitHub Pages 工作流。
- 遗留事项：后续审核页面源码中的公开内容与外链，再分批提交；源码完整前不启用 Pages 工作流。

### v01.02 — 2026-07-24

- 修改范围：GitHub 第一版安全上传。
- 具体变化：准备创建公开仓库 `zhechun333`；第一版仅上传 Git 基础规则、AI 协作规范、项目概述和项目进度表，不上传源码、作品截图、媒体素材、联系邮箱或 Pages 工作流。
- 验证结果：待提交范围已限制为 `.gitignore`、`.gitattributes`、`AGENTS.md`、`项目概述.md` 和 `项目进度表.md`；文本敏感信息扫描未发现私人联系方式、密钥、本机绝对路径或旧仓库地址。
- 遗留事项：后续分批审核并上传源码与必要素材；正式启用 Pages 前将 Vite 基础路径调整为 `/zhechun333/`，再上传部署工作流。

### v01.01 — 2026-07-24

- 修改范围：协作文档迁移。
- 具体变化：以旧《个人作品集网站制作进度与交接说明》和《网站制作进度与功能说明》为来源，重新整理 `项目概述.md`；迁移项目定位、技术栈、页面结构、现有功能、核心交互、视觉偏好、素材职责、维护入口、验收要求和待办；将旧文档中有日期的关键修改整理为历史版本记录；剔除过期实现、已删除素材、私人联系方式、旧仓库地址和本机路径。
- 验证结果：新概述与当前源码、依赖、Vite 基础路径和 Pages 工作流核对一致；生产构建通过。
- 遗留事项：首次提交前人工复核作品截图；创建并绑定 GitHub 远程仓库。

### v01.00 — 2026-07-24

- 修改范围：Git 初始化、上传安全和协作规则。
- 具体变化：初始化 `main` 分支仓库；设置仓库级 noreply 提交邮箱；建立文档同步规则；排除依赖、构建产物、日志、本地旧文档和未使用素材；清理发布目录中的未引用资源。
- 验证结果：生产构建成功；发布目录未检测到未引用文件；待上传文本未发现手机号、微信号、密钥或本机绝对路径。
- 遗留事项：绑定 GitHub 远程仓库并完成首次提交。

### v00.07 — 2026-07-20

- 修改范围：经历区互动信息面板。
- 具体变化：个人照片增加轻微指针景深与柔光反馈；加入“运营 / 策划 / 产品”能力切换；优化平板和手机布局；统一使用系统宋体栈。
- 验证结果：旧记录显示构建通过，并完成响应式调整。
- 遗留事项：后续改动继续复核照片高度与移动端占屏比例。

### v00.06 — 2026-07-19

- 修改范围：图片灯箱与 AI 辅助视觉展示。
- 具体变化：灯箱支持点击图片外空白区域关闭；品牌自媒体项目加入 AI 辅助视觉说明和四张示例图，并接入统一灯箱。
- 验证结果：旧记录确认关闭按钮、背景点击和 `Escape` 均可使用。
- 遗留事项：持续检查新增图片的公开信息和移动端显示。

### v00.05 — 2026-07-17

- 修改范围：导航、滚动反馈和首屏素材。
- 具体变化：加入当前板块高亮、`aria-current`、页内进度线、视口显现和返回顶部；首页移除旧 HLS 视频覆盖，改用静态视觉素材；新增动效兼容低动态模式。
- 验证结果：旧记录确认页面不再加载 `pinterest-loop`；相关旧文件已在后续清理。
- 遗留事项：无。

### v00.04 — 2026-07-16

- 修改范围：优势模块和全站底色。
- 具体变化：优势区改为六步内容运营流程和横向能力信息栏；桌面、平板和手机使用不同排列；非深色区域统一为低饱和浅绿渐变，保留暖白和浅金层次。
- 验证结果：旧记录确认移动端流程改为纵向，照片展示宽度同步收紧。
- 遗留事项：补充更具体的 AI 使用内容。

### v00.03 — 2026-07-14

- 修改范围：项目轮播、灯箱和原始作品链接。
- 具体变化：项目主图接入整图灯箱，支持键盘与左右切换；轮播遮罩和控制样式优化；将原始作品链接按八个项目及平台账号分组展示，并统一新窗口安全属性。
- 验证结果：旧记录确认灯箱、缩略图和链接响应式布局可用。
- 遗留事项：核对外部短链接是否可以统一升级为 HTTPS。

### v00.02 — 2026-07-12

- 修改范围：GitHub Pages 部署路径。
- 具体变化：采用 Vite 基础路径处理项目子目录资源；加入 GitHub Actions Pages 构建与发布流程；明确 `dist/` 不提交。
- 验证结果：部署结构完成，当前基础路径后续统一为 `/zhechunSpace/`。
- 遗留事项：创建最终远程仓库后再次核对仓库名与基础路径一致。

## 记录模板

### vXX.XX — YYYY-MM-DD

- 修改范围：
- 具体变化：
- 验证结果：
- 遗留事项：
