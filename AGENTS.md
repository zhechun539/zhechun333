# AI 协作与项目前端规范

## 开始工作前

任何 AI 或开发者修改仓库前，必须完整阅读：

1. `项目概述.md`
2. `项目进度表.md`

以这两份文档记录的当前状态为准。发现文档与代码不一致时，先核对实际实现，再在本次修改中修正文档，禁止继续传播已过期信息。

## 文档同步规则

- 每次完成一组有独立维护价值的修改后，在 `项目进度表.md` 顶部追加一条版本记录；同一目标下的连续微调合并记录，不为每次尝试、像素调整或验收动作单独建版本。
- 版本号使用 `vXX.XX` 格式，从当前版本顺序递增；普通修改递增末两位，重大结构调整可递增前两位并将末两位归零。
- 进度记录只保留对后续维护有用的信息：最终结果、影响范围、关键约束、迁移事项、未解决风险。不要记录命令输出、逐视口尺寸、反复试错、实现过程或已被后续版本覆盖的中间状态。
- 单条记录采用类似 Git 提交日志的格式：`type(scope): 一句话结果`。类型优先使用 `feat`、`fix`、`refactor`、`docs`、`chore`；仅在影响后续判断时补一行“说明”“约束”或“待办”，没有内容就省略。
- 每个已发布版本至少保留一条日志，不删除或只用版本区间替代；允许另加阶段摘要帮助阅读，但阶段摘要不能取代版本流水。`项目概述.md` 负责描述当前系统，进度表不重复抄写功能全貌。
- 涉及功能、页面结构、技术栈、构建部署、目录职责、核心交互或公开内容变化时，必须同步更新 `项目概述.md`。
- 不得在协作文档中记录私人邮箱、手机号、微信号、访问令牌、本机绝对路径或其他不应公开的信息。
- 完成工作前，检查代码、`项目概述.md` 与 `项目进度表.md` 三者一致。

## Git 与安全

- 提交前检查 `git status`，确认没有 `.env`、凭据、日志、构建产物、依赖目录或本地隔离文件进入提交。
- `node_modules/`、`dist/`、`.local-docs/` 和 `.local-unused-assets/` 不得提交。
- 禁止在源码、工作流或文档中写入密码、Token、私钥和个人联系方式。
- GitHub Pages 发布路径以 `vite.config.js` 的 `base` 为准；修改仓库名或域名时必须同步验证资源路径。
- 上传Git时，注意安全上传，避免造成风控导致封号。

# 项目前端视觉与动效规范

## 强制调用的 Skills

凡任务涉及页面视觉、布局、组件样式、交互、滚动效果、过渡、微动效、响应式表现或视觉素材，必须按以下顺序使用对应 skill，并在开始工作时说明本次使用了哪些 skill：

1. `ui-ux-pro-max`
   - 每次视觉或动效任务都必须先调用。
   - 负责确定或复核设计系统、信息层级、排版、配色、间距、可访问性、响应式规则及动效原则。
   - 若项目已有明确视觉系统，应以现有系统为基础进行检查和补充，不得无故整体换风格。

2. `bexa`
   - 每次视觉或动效任务都必须调用。
   - 负责把设计方向落实为高品质、非模板化的前端界面和动画。
   - 动画必须服务于状态变化、空间关系、内容叙事或操作反馈；禁止为了“看起来会动”而添加无意义的漂浮、旋转、发光、缩放和循环动画。
   - 根据项目现有技术栈选择 CSS、Web Animations API、GSAP、Framer Motion 或 Three.js；引入第三方依赖前必须先检查项目依赖。

3. `imagegen`
   - 每次视觉任务都必须评估是否需要调用。
   - 仅当页面确实需要原创位图素材，例如封面图、背景纹理、插画、产品图、视觉隐喻或图片编辑时才实际生成或编辑图片。
   - 简单形状、图标、渐变、光效、布局背景以及可用 HTML/CSS/SVG/Canvas 稳定实现的视觉，不得调用 `imagegen`。
   - 项目使用的最终图片必须保存到项目目录并更新代码引用，不能只保留在 Codex 的生成目录中。

4. `browser:control-in-app-browser`
   - 每次完成视觉或动效实现后都必须调用并进行实际页面验收。
   - 至少检查桌面端和移动端布局、首次加载、滚动、悬停或点击反馈、动画衔接、控制台错误以及明显卡顿。
   - 动画改动后必须验证 `prefers-reduced-motion`；本地页面不支持热更新时，修改代码后应重新加载再检查。
   - 如果浏览器中没有可访问的项目页面，应先尝试用项目现有启动方式运行；确实无法运行时，明确报告未完成浏览器验收及原因，不得声称效果已验证。

## 冲突处理与优先级

- 用户当前明确要求 > 项目现有品牌与设计规范 > `ui-ux-pro-max` 的设计系统建议 > `bexa` 的默认审美偏好。
- `ui-ux-pro-max` 负责“应该采用什么体验与视觉规则”，`bexa` 负责“如何高质量实现”，两者不相互替代。
- `imagegen` 只提供位图素材，不负责决定页面布局、组件结构或动画编排。
- 浏览器验收结果优先于静态代码判断；如实际效果与设计预期冲突，应根据实测调整实现。
- 不得为满足某个 skill 的示例默认值而破坏项目已有技术栈、内容、品牌风格或性能目标。

## 动效质量底线

- 每个页面只突出 1–2 类核心动效语言，保持节奏一致。
- 微交互通常控制在 150–300ms；入场、页面切换和滚动叙事可按内容需要延长，但不得拖沓。
- 优先使用 `transform` 和 `opacity`，避免高频动画触发布局重排。
- 禁止同时在同一组件中混用 GSAP 与 Framer Motion。
- 所有持续动画、事件监听和时间线必须正确清理。
- 必须支持 `prefers-reduced-motion`，且关闭或减少动画后内容仍完整可用。
- 移动端不得机械复制桌面端的重视差、跟随光标或高成本 WebGL 效果。
- 最终交付前检查动画是否自然、有因果、有层级、不卡顿，并清理实验性或重复效果。
